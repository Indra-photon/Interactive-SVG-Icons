{
  "slug": "home",
  "name": "Home Icon",
  "category": "actions",
  "tags": ["home", "house", "main", "dashboard"],
  "description": "Icon for selecting the home page",
  "createdAt": "2026-02-11T00:00:00Z",
  "variations": [
    {
      "name": "default",
      "displayName": "Default",
      "tier": "free",
      "description": "Click the icon to toggle the selected state. The icon will animate to indicate selection.",
      "animationType": "select",
      "dependencies": ["motion/react"],
      "props": [
        {
          "name": "size",
          "type": "number",
          "default": "24",
          "required": false,
          "description": "Width and height of the icon in pixels"
        },
        {
          "name": "color",
          "type": "string",
          "default": "'currentColor'",
          "required": false,
          "description": "Stroke color of the icon (CSS color value)"
        },
        {
          "name": "className",
          "type": "string",
          "default": "''",
          "required": false,
          "description": "Additional CSS classes to apply"
        },
        {
          "name": "isSelected",
          "type": "boolean",
          "default": "false",
          "required": false,
          "description": "Controls the selected state of the icon."
        }
      ]
    },
    {
  "name": "draw",
  "displayName": "Sketch Reveal",
  "tier": "free",
  "description": "Stroke animation draws the house from foundation to roof during loading",
  "animationType": "loading",
  "dependencies": ["motion/react"],
  "props": [
    {
      "name": "size",
      "type": "number",
      "default": "24",
      "required": false,
      "description": "Width and height of the icon in pixels"
    },
    {
      "name": "color",
      "type": "string",
      "default": "'currentColor'",
      "required": false,
      "description": "Stroke color of the icon (CSS color value)"
    },
    {
      "name": "className",
      "type": "string",
      "default": "''",
      "required": false,
      "description": "Additional CSS classes to apply"
    },
    {
      "name": "isLoading",
      "type": "boolean",
      "default": "false",
      "required": false,
      "description": "Controls the sketch reveal animation state. Set to true during loading operations."
    }
  ]
}
  ]
}