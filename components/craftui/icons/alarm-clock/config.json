{
  "slug": "alarm",
  "name": "Alarm Icon",
  "category": "time",
  "tags": [
    "alarm",
    "clock",
    "time",
    "alert",
    "notification",
    "schedule"
  ],
  "description": "Filled alarm clock icon with multiple animation variations",
  "createdAt": "2026-02-17T00:00:00Z",
  "variations": [
    {
      "name": "default",
      "displayName": "Ring",
      "componentName": "IconAlarmRing",
      "tier": "free",
      "description": "The alarm rocks left and right like a ringing bell, with ears counter-rotating to sell the physics",
      "animationType": "hover",
      "dependencies": [
        "motion"
      ],
      "props": [
        {
          "name": "size",
          "type": "number",
          "default": 24,
          "required": false,
          "description": "Width and height of the icon in pixels",
          "min": 8,
          "max": 256,
          "step": 1
        },
        {
          "name": "color",
          "type": "string",
          "default": "currentColor",
          "required": false,
          "description": "Fill color of the icon (CSS color value)"
        },
        {
          "name": "className",
          "type": "string",
          "default": "",
          "required": false,
          "description": "Additional CSS classes to apply to the SVG element"
        },
        {
          "name": "isHovered",
          "type": "boolean",
          "default": false,
          "required": false,
          "description": "Triggers the ring animation. Manage this state in the parent via onMouseEnter/onMouseLeave."
        },
        {
          "name": "duration",
          "type": "number",
          "default": 0.9,
          "required": false,
          "description": "Duration of the full rocking oscillation in seconds",
          "min": 0.2,
          "max": 3,
          "step": 0.05
        },
        {
          "name": "ease",
          "type": "ease",
          "default": "easeInOut",
          "required": false,
          "description": "Easing applied between each rock keyframe. Accepts a named ease or a cubic-bezier array."
        }
      ]
    },
    {
      "name": "tick",
      "displayName": "Tick",
      "componentName": "IconAlarmTick",
      "tier": "free",
      "description": "The clock hand sweeps a full clockwise rotation on click, like dialing an alarm to confirm it's set",
      "animationType": "click",
      "dependencies": [
        "motion"
      ],
      "props": [
        {
          "name": "size",
          "type": "number",
          "default": 24,
          "required": false,
          "description": "Width and height of the icon in pixels",
          "min": 8,
          "max": 256,
          "step": 1
        },
        {
          "name": "color",
          "type": "string",
          "default": "currentColor",
          "required": false,
          "description": "Fill color of the icon (CSS color value)"
        },
        {
          "name": "className",
          "type": "string",
          "default": "",
          "required": false,
          "description": "Additional CSS classes to apply to the SVG element"
        },
        {
          "name": "isAnimating",
          "type": "boolean",
          "default": false,
          "required": false,
          "description": "Triggers the hand sweep externally. The icon also fires the animation on direct click."
        },
        {
          "name": "onAnimationComplete",
          "type": "function",
          "default": "undefined",
          "required": false,
          "description": "Callback fired after the hand completes its full sweep."
        },
        {
          "name": "duration",
          "type": "number",
          "default": 0.6,
          "required": false,
          "description": "Base sweep duration in seconds; a random amount (up to ~0.5s) is added per spin",
          "min": 0.2,
          "max": 2,
          "step": 0.05
        },
        {
          "name": "ease",
          "type": "ease",
          "default": [0.4, 0, 0.2, 1],
          "required": false,
          "description": "Easing of the hand sweep. Accepts a named ease or a cubic-bezier array."
        }
      ]
    }
  ]
}