{
  "name": "Scan Line",
  "category": "loaders",
  "tags": [
    "bar",
    "scan",
    "gradient",
    "linear",
    "progress",
    "sweep",
    "trail"
  ],
  "description": "A gradient sweep travels across a thin bar from left to right, then resets and repeats",
  "createdAt": "2026-05-14",
  "variations": [
    {
      "name": "default",
      "displayName": "Scan Line",
      "tier": "free",
      "description": "A glowing gradient segment \u2014 bright at the leading edge, fading behind \u2014 sweeps across a faint track and resets",
      "animationType": "loop",
      "dependencies": [
        "framer-motion"
      ],
      "props": [
        {
          "name": "width",
          "type": "number",
          "default": 200,
          "description": "Width of the bar in pixels"
        },
        {
          "name": "height",
          "type": "number",
          "default": 4,
          "description": "Thickness of the bar in pixels"
        },
        {
          "name": "color",
          "type": "string",
          "default": "currentColor",
          "description": "Color of the gradient sweep"
        },
        {
          "name": "trailLength",
          "type": "number",
          "default": 80,
          "description": "Length of the gradient segment in pixels"
        },
        {
          "name": "isAnimating",
          "type": "boolean",
          "default": true,
          "description": "Controls whether the animation runs. Set to false to pause animation."
        },
        {
          "name": "duration",
          "type": "number",
          "default": 1.4,
          "description": "Animation cycle duration in seconds",
          "min": 0.1,
          "max": 8,
          "step": 0.1
        },
        {
          "name": "ease",
          "type": "ease",
          "default": "linear",
          "description": "Easing function"
        },
        {
          "name": "repeatDelay",
          "type": "number",
          "default": 0.4,
          "description": "Pause between cycles in seconds",
          "min": 0,
          "max": 5,
          "step": 0.1
        }
      ],
      "componentName": "ScanLine"
    }
  ]
}