{
  "slug": "alert-dialog",
  "name": "Alert Dialog",
  "category": "overlay",
  "shadcnBase": "alert-dialog",
  "tags": [
    "alert-dialog",
    "dialog",
    "modal",
    "overlay",
    "confirmation",
    "base-ui",
    "animation",
    "clip-path",
    "spring",
    "blur"
  ],
  "description": "A confirmation dialog built on Base UI's accessible AlertDialog primitive. Every variation uses a different entrance technique — scale, blur focus-in, spring overshoot, iOS zoom, CRT unroll, a parting curtain, or an action-reactive exit that branches on Agree vs Disagree. All are theme-token driven so they flip cleanly between light and dark, and every surface is responsive with type that scales up on larger screens.",
  "createdAt": "2026-07-26T00:00:00Z",
  "variations": [
    {
      "name": "scale",
      "displayName": "Scale",
      "componentName": "ScaleAlertDialog",
      "tier": "free",
      "animationType": "ease",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The popup scales up from 95% while fading in; adjust the duration with the DialKit panel.",
      "features": [
        "Enters by scaling the popup from 95% to 100% while cross-fading opacity, driven by the data-[starting-style] hook on an ease-out tween — compositor-only (transform + opacity)",
        "Breaks enter/exit symmetry: the exit drops the scale entirely and fades out on a fixed ~70ms opacity tween, decoupled from the enter with an important-flagged duration so tuning entrance speed never slows the dismiss",
        "Cross-fades the backdrop independently of the popup — a longer fade-in, a ~70ms fade-out — so the motion stays anchored on the card",
        "Renders fully from theme tokens (popover / border / primary / muted) and adapts responsively: full-width with column-stacked buttons on mobile, capped at max-w-md with a right-aligned row and a stepped-up title on ≥sm",
        "Inherits Base UI AlertDialog semantics — focus trap, focus restoration on close, role=\"alertdialog\", and aria-labelledby / aria-describedby wired from Title and Description; the trigger carries a focus-visible ring"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 110,
          "min": 80,
          "max": 800,
          "step": 10,
          "description": "Enter/exit transition duration in milliseconds"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "Keep editing",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "Discard",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "blur",
      "displayName": "Blur",
      "componentName": "BlurAlertDialog",
      "tier": "free",
      "animationType": "ease",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The card resolves out of a 16px blur while fading in — like a lens pulling focus.",
      "features": [
        "Resolves the popup out of a 16px blur while fading in — a state-driven transition on the filter and opacity properties (a filter/paint effect, not compositor-only), like a lens pulling focus; the exit reverses back into blur",
        "Layers a frosted-glass surface (translucent fill + backdrop-blur) tuned per theme through dark: overrides, with two ambient fuchsia / sky color blobs sitting behind the glass via a large blur radius for depth without touching content contrast",
        "Drives the blur-in duration from a duration prop; the exit mirrors the enter",
        "Aligns the primary action to the leading edge, sets body copy to relaxed leading, and gives the buttons a concentric rounded-xl against the rounded-3xl surface; padding and button stacking adapt on mobile",
        "Inherits Base UI AlertDialog semantics — focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description; the trigger shows a focus-visible ring"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 130,
          "min": 100,
          "max": 200,
          "step": 10,
          "description": "Blur focus-in duration in milliseconds"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "Stay",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "Sign out",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "slam",
      "displayName": "Slam",
      "componentName": "SlamAlertDialog",
      "tier": "free",
      "animationType": "spring",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The card drops in with a back-out spring, overshooting before it settles.",
      "features": [
        "Splits motion across two layers: the outer popup cross-fades opacity while an inner wrapper carries the transform via group-data-[starting-style] / [ending-style] hooks, keeping the animation off the centering translate",
        "Enters on a back-out (overshoot) cubic-bezier — the inner card scales up from 80% with a -40px translateY, overshooting before it settles; the enter duration is a duration prop",
        "Breaks enter/exit symmetry: the exit shrinks to 96% on a plain ease-out with no overshoot at a fixed ~160ms, so dismissal is quieter and faster than the slam-in",
        "Renders the neo-brutalist look with hard offset box-shadows and borders in var(--foreground) so the whole block inverts in dark mode; the yellow / lime accent buttons translate down and drop their shadow on the active press state",
        "Accessibility: honors prefers-reduced-motion (motion-reduce guards zero the transitions on backdrop, popup, and inner card so enter and exit are instant) and inherits Base UI's focus trap, focus restore, role=\"alertdialog\", and Title / Description-driven aria labelling"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 320,
          "min": 200,
          "max": 900,
          "step": 10,
          "description": "Overshoot slam (enter) duration in milliseconds"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "Nope",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "Do it!",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "ios",
      "displayName": "iOS",
      "componentName": "IosAlertDialog",
      "tier": "free",
      "animationType": "spring",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The card enters at 118% and springs down to rest, iOS system-alert style.",
      "features": [
        "Mirrors the iOS system alert: an inner card zooms from 118% down to rest on a back-out (overshoot) cubic-bezier while the outer popup cross-fades — motion split across two layers via group-data hooks, with the spring-settle enter duration driven by a duration prop",
        "Breaks enter/exit symmetry: the exit shrinks to 95% on a plain ease-out with no overshoot at ~200ms, faster than the ~350ms spring-in",
        "Renders a frosted popover surface (translucent fill + backdrop-blur) at a fixed 270px width with hairline dividers between stacked full-width buttons; the action reads in iOS blue with an active-press background tint",
        "Respects prefers-reduced-motion (motion-reduce guards make enter and exit instant) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 350,
          "min": 150,
          "max": 800,
          "step": 10,
          "description": "Spring settle (enter) duration in milliseconds"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "Don't Allow",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "Allow",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "terminal",
      "displayName": "Terminal",
      "componentName": "TerminalAlertDialog",
      "tier": "free",
      "animationType": "clip-path",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The popup unrolls vertically from a center line, CRT power-on style.",
      "features": [
        "Powers on like a CRT: clip-path unrolls the popup from inset(50% 0 50% 0) — a collapsed center line — out to inset(0) on an ease-out cubic-bezier, while opacity ramps in over roughly the first 40% of the clip so the reveal reads as the screen warming up (clip-path is a paint-level animation, not compositor-only)",
        "Breaks enter/exit symmetry: the exit re-rolls to the center line on a decoupled, faster ~220ms transition",
        "Keeps a self-contained green-on-black identity regardless of the site theme (only the trigger uses theme tokens); includes monospace type, faux traffic-light window dots, a $-prompt title with a blinking block cursor, and a [WARN]-tagged description",
        "Respects prefers-reduced-motion on two fronts — motion-reduce zeroes the unroll transition on popup and backdrop, and stops the infinite cursor pulse via animate-none — and inherits Base UI's focus trap, role=\"alertdialog\", and Title / Description aria labelling"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 350,
          "min": 150,
          "max": 900,
          "step": 10,
          "description": "Clip-path unroll (enter) duration in milliseconds"
        },
        {
          "name": "title",
          "type": "string",
          "description": "Command shown after the prompt"
        },
        {
          "name": "description",
          "type": "string",
          "description": "Warning body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "[esc] abort",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "[y] execute",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "curtain",
      "displayName": "Curtain",
      "componentName": "CurtainAlertDialog",
      "tier": "free",
      "animationType": "clip-path",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open. The popup parts horizontally from its center line like a stage curtain.",
      "features": [
        "Parts like a stage curtain: clip-path opens the popup horizontally from inset(0 50% 0 50%) — a collapsed center line — out to inset(0) on an ease-out cubic-bezier; no opacity is animated, only the clip (a paint-level animation, not compositor-only)",
        "Breaks enter/exit symmetry: the curtain draws shut on a decoupled, faster transition than it opens",
        "Sets an editorial treatment — serif title and body, an uppercase letter-spaced eyebrow, and a short hairline rule — on full theme-token surfaces so the paper flips to ink in dark mode, with type stepping up from text-xl to text-2xl on ≥sm",
        "Respects prefers-reduced-motion (motion-reduce zeroes the clip transition on popup and backdrop) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and Title / Description-driven aria labelling"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 400,
          "min": 200,
          "max": 1200,
          "step": 10,
          "description": "Clip-path curtain (enter) duration in milliseconds"
        },
        {
          "name": "eyebrow",
          "type": "string",
          "default": "Confirmation",
          "description": "Small uppercase label above the title"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "cancelLabel",
          "type": "string",
          "default": "Never mind",
          "description": "Dismiss button label"
        },
        {
          "name": "actionLabel",
          "type": "string",
          "default": "Confirm cancellation",
          "description": "Confirm button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when the action button is pressed"
        }
      ]
    },
    {
      "name": "reactive",
      "displayName": "Reactive",
      "componentName": "ReactiveAlertDialog",
      "tier": "free",
      "animationType": "spring",
      "dependencies": ["@base-ui-components/react"],
      "registryDependencies": [],
      "previewHint": "Click the trigger to open, then choose. Agree exits with a snappy scale-down; Disagree tips from its top-left corner and tumbles away.",
      "features": [
        "Branches the exit on the action: a controlled Base UI AlertDialog (open / onOpenChange) plus an exit-mode state set before close pick which closing animation runs, so the same dialog leaves two different ways",
        "Enters by scaling the inner card from 95% with an opacity fade on an ease-out curve; Agree mirrors that exactly — a snappy scale-down and fade — so accepting reverses the entrance",
        "Disagrees with a tumble: the card pivots from transform-origin top-left, rotating and dropping via translateY before fading, on an ease-in ('gravity') cubic-bezier that accelerates as it falls; the tumble's angle, drop distance, and duration are props",
        "Splits centering from animation across two layers (outer popup translates to center and fades, inner card carries the transform) so the exit rotate/translate never fights the centering translate; the exit mode resets to neutral on every open, keeping entrances and Escape / backdrop dismissals snappy",
        "Respects prefers-reduced-motion (motion-reduce guards make both exits instant) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description"
      ],
      "props": [
        {
          "name": "duration",
          "type": "number",
          "default": 200,
          "min": 80,
          "max": 500,
          "step": 10,
          "description": "Enter / Agree-exit duration in milliseconds"
        },
        {
          "name": "disagreeDuration",
          "type": "number",
          "default": 150,
          "min": 100,
          "max": 200,
          "step": 5,
          "description": "Disagree tumble-away duration in milliseconds"
        },
        {
          "name": "disagreeAngle",
          "type": "number",
          "default": 3,
          "min": 0,
          "max": 25,
          "step": 1,
          "description": "Disagree rotation in degrees"
        },
        {
          "name": "disagreeShift",
          "type": "number",
          "default": 220,
          "min": 0,
          "max": 240,
          "step": 5,
          "description": "Disagree vertical drop in pixels"
        },
        { "name": "title", "type": "string", "description": "Dialog title" },
        {
          "name": "description",
          "type": "string",
          "description": "Supporting body copy"
        },
        {
          "name": "disagreeLabel",
          "type": "string",
          "default": "Disagree",
          "description": "Reject button label"
        },
        {
          "name": "agreeLabel",
          "type": "string",
          "default": "Agree",
          "description": "Accept button label"
        },
        {
          "name": "onAction",
          "type": "() => void",
          "description": "Fires when Agree is pressed"
        }
      ]
    }
  ]
}
