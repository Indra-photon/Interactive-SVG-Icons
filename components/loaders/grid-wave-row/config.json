{
  "name": "Grid Wave Row",
  "category": "loaders",
  "tags": ["grid", "wave", "row", "dots", "matrix", "sequential"],
  "description": "3×3 grid sweeps left to right row by row — top row first, bottom row last",
  "createdAt": "2026-05-14",
  "variations": [
    {
      "name": "default",
      "displayName": "Grid Wave Row",
      "tier": "free",
      "description": "Each row sweeps left to right in sequence, creating a horizontal wave that cascades downward",
      "animationType": "loop",
      "dependencies": ["framer-motion"],
      "props": [
        { "name": "width", "type": "number", "default": 100, "description": "Width of the loader in pixels" },
        { "name": "height", "type": "number", "default": 100, "description": "Height of the loader in pixels" },
        { "name": "color", "type": "string", "default": "currentColor", "description": "Fill color of the dots" },
        { "name": "dotSize", "type": "number", "default": 16, "description": "Diameter of each dot in pixels" },
        { "name": "isAnimating", "type": "boolean", "default": true, "description": "Controls whether the animation runs" }
      ]
    }
  ]
}
