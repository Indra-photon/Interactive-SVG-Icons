{
  "name": "Grid Random",
  "category": "loaders",
  "tags": [
    "grid",
    "random",
    "dots",
    "matrix",
    "scatter",
    "noise"
  ],
  "description": "3\u00d73 grid dots fire in a pre-seeded random order, creating an unpredictable scatter pattern",
  "createdAt": "2026-05-14",
  "variations": [
    {
      "name": "default",
      "displayName": "Grid Random",
      "tier": "free",
      "description": "Dots activate in a fixed but non-sequential order that reads as random \u2014 like a matrix initialising",
      "animationType": "loop",
      "dependencies": [
        "framer-motion"
      ],
      "props": [
        {
          "name": "width",
          "type": "number",
          "default": 100,
          "description": "Width of the loader in pixels"
        },
        {
          "name": "height",
          "type": "number",
          "default": 100,
          "description": "Height of the loader in pixels"
        },
        {
          "name": "color",
          "type": "string",
          "default": "currentColor",
          "description": "Fill color of the dots"
        },
        {
          "name": "dotSize",
          "type": "number",
          "default": 16,
          "description": "Diameter of each dot in pixels"
        },
        {
          "name": "isAnimating",
          "type": "boolean",
          "default": true,
          "description": "Controls whether the animation runs"
        },
        {
          "name": "duration",
          "type": "number",
          "default": 1.4,
          "description": "Animation cycle duration in seconds",
          "min": 0.1,
          "max": 8,
          "step": 0.1
        },
        {
          "name": "ease",
          "type": "ease",
          "default": "easeInOut",
          "description": "Easing function"
        },
        {
          "name": "gridSize",
          "type": "number",
          "default": 3,
          "min": 2,
          "max": 6,
          "step": 1,
          "description": "Number of dots per row and column (n \u00d7 n grid)"
        }
      ],
      "componentName": "GridRandom"
    }
  ]
}