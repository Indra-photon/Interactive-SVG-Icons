{
  "name": "Audio Bars Glow",
  "category": "loaders",
  "tags": [
    "audio",
    "music",
    "sound",
    "glow",
    "gradient",
    "vertical",
    "bars"
  ],
  "description": "Six synchronized vertical bars with gradient and glow effect",
  "createdAt": "2026-02-20",
  "variations": [
    {
      "name": "default",
      "displayName": "Audio Bars Glow",
      "tier": "free",
      "description": "Six vertical bars with gradient fill, glow effect, and synchronized alternating animation",
      "animationType": "loop",
      "dependencies": [
        "framer-motion"
      ],
      "props": [
        {
          "name": "width",
          "type": "number",
          "default": 60,
          "description": "Width of the loader container in pixels"
        },
        {
          "name": "height",
          "type": "number",
          "default": 60,
          "description": "Height of the loader container in pixels"
        },
        {
          "name": "color",
          "type": "string",
          "default": "#3b82f6",
          "description": "Base color for the gradient (hex, rgb, or color name)"
        },
        {
          "name": "barWidth",
          "type": "number",
          "default": 4,
          "description": "Width of each individual bar in pixels"
        },
        {
          "name": "gap",
          "type": "number",
          "default": 3,
          "description": "Gap between bars in pixels"
        },
        {
          "name": "isAnimating",
          "type": "boolean",
          "default": true,
          "description": "Controls whether the animation runs. Set to false to pause animation."
        },
        {
          "name": "duration",
          "type": "number",
          "default": 0.8,
          "description": "Animation cycle duration in seconds",
          "min": 0.1,
          "max": 8,
          "step": 0.1
        },
        {
          "name": "ease",
          "type": "ease",
          "default": "easeInOut",
          "description": "Easing function"
        }
      ],
      "componentName": "AudioBarsGlow"
    }
  ]
}