{
  "name": "Spotlight",
  "category": "loaders",
  "tags": [
    "spotlight",
    "glow",
    "ambient",
    "radial",
    "beam",
    "drift",
    "light"
  ],
  "description": "A soft radial gradient oval drifts from left to right like a light beam, then resets",
  "createdAt": "2026-05-14",
  "variations": [
    {
      "name": "default",
      "displayName": "Spotlight",
      "tier": "free",
      "description": "An ambient spotlight beam drifts slowly across the container \u2014 bright at center, fading to nothing at the edges \u2014 then reappears from the left",
      "animationType": "loop",
      "dependencies": [
        "framer-motion"
      ],
      "props": [
        {
          "name": "width",
          "type": "number",
          "default": 200,
          "description": "Width of the loader container in pixels"
        },
        {
          "name": "height",
          "type": "number",
          "default": 60,
          "description": "Height of the loader container in pixels"
        },
        {
          "name": "color",
          "type": "string",
          "default": "currentColor",
          "description": "Color of the spotlight beam"
        },
        {
          "name": "isAnimating",
          "type": "boolean",
          "default": true,
          "description": "Controls whether the animation runs. Set to false to pause animation."
        },
        {
          "name": "duration",
          "type": "number",
          "default": 2.0,
          "description": "Animation cycle duration in seconds",
          "min": 0.1,
          "max": 8,
          "step": 0.1
        },
        {
          "name": "ease",
          "type": "ease",
          "default": "easeInOut",
          "description": "Easing function"
        },
        {
          "name": "repeatDelay",
          "type": "number",
          "default": 0.6,
          "description": "Pause between cycles in seconds",
          "min": 0,
          "max": 5,
          "step": 0.1
        }
      ],
      "componentName": "Spotlight"
    }
  ]
}