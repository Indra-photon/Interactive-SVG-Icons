{
  "components": [
    {
      "slug": "otp-input",
      "name": "OTP Input",
      "category": "input",
      "tags": [
        "otp",
        "input",
        "verification",
        "2fa",
        "one-time-code",
        "ripple",
        "svg",
        "caret",
        "form"
      ],
      "description": "One-time password input with a living SVG ripple border. Every keystroke nudges the outline, correct codes send a green wave across the boxes, wrong codes bounce red and shake. Validation, carets and all motion parameters are prop-driven.",
      "createdAt": "2026-07-07T00:00:00Z",
      "variations": [
        {
          "name": "ripple",
          "displayName": "Ripple",
          "componentName": "RippleOtp",
          "tier": "free",
          "description": "Each box wears an SVG outline that behaves like a liquid surface — typing nudges it, moving to the next box hands the ripple off toward its right edge. Pass a validate function and the component choreographs the pending ring, the staggered success wave and the red shake by itself, then calls your onSuccess / onFailure.",
          "animationType": "ripple",
          "dependencies": [
            "motion/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "length",
              "type": "number",
              "default": 6,
              "min": 4,
              "max": 8,
              "step": 1,
              "description": "Number of character boxes"
            },
            {
              "name": "mode",
              "type": "select",
              "options": [
                "numeric",
                "alphanumeric"
              ],
              "default": "numeric",
              "description": "Which characters the boxes accept — alphanumeric uppercases as you type"
            },
            {
              "name": "masked",
              "type": "boolean",
              "default": false,
              "description": "Render entered characters as dots"
            },
            {
              "name": "rippleOnType",
              "type": "boolean",
              "default": false,
              "description": "Animate the border on every keystroke — off by default, so the ripple only plays for pending / success / failure"
            },
            {
              "name": "pendingRing",
              "type": "boolean",
              "default": false,
              "description": "Oscillating ring while an async validate is pending — off by default, pending shows as border color only"
            },
            {
              "name": "caretColor",
              "type": "string",
              "default": "#3B82F6",
              "description": "Caret color — drives the native bar caret and the fake block / underscore carets"
            },
            {
              "name": "caretShape",
              "type": "select",
              "options": [
                "bar",
                "block",
                "underscore"
              ],
              "default": "bar",
              "description": "Caret style — bar is the native caret; block and underscore are rendered carets with a native-feeling blink"
            },
            {
              "name": "staggerMs",
              "type": "number",
              "default": 70,
              "min": 30,
              "max": 200,
              "step": 5,
              "description": "Delay between each box's green pulse in the success wave, in ms"
            },
            {
              "name": "maxAmplitude",
              "type": "number",
              "default": 5,
              "min": 3,
              "max": 12,
              "step": 0.5,
              "description": "Ripple height ceiling in px"
            },
            {
              "name": "bumpWidth",
              "type": "number",
              "default": 0.1,
              "min": 0.05,
              "max": 0.25,
              "step": 0.01,
              "description": "How localized the ripple bulge is, as a fraction of the box perimeter"
            },
            {
              "name": "decay",
              "type": "number",
              "default": 0.06,
              "min": 0.02,
              "max": 0.2,
              "step": 0.005,
              "description": "How fast ripples settle — higher is snappier"
            },
            {
              "name": "radius",
              "type": "number",
              "default": 14,
              "min": 4,
              "max": 22,
              "step": 1,
              "description": "Corner radius in px, shared by the box and its ripple outline"
            },
            {
              "name": "strokeWidth",
              "type": "number",
              "default": 1.5,
              "min": 1,
              "max": 3,
              "step": 0.25,
              "description": "Ripple outline stroke width in px"
            },
            {
              "name": "shakeDistance",
              "type": "number",
              "default": 6,
              "min": 2,
              "max": 14,
              "step": 1,
              "description": "Failure shake travel in px"
            },
            {
              "name": "validate",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => boolean | Promise<boolean> — called when all boxes are filled. Return or resolve true for success, false for failure; the component shows the pending border color while a promise is in flight, then plays the result animation"
            },
            {
              "name": "onSuccess",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired after the green success wave finishes. Put your post-verification action here (navigate, submit, close modal)"
            },
            {
              "name": "onFailure",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired after the red pulse and shake finish. By default the boxes have already been cleared and refocused (see autoClearOnFailure)"
            },
            {
              "name": "onComplete",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired once every time all boxes become filled, before validate runs"
            },
            {
              "name": "onChange",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(value: string) => void — fired on every edit with the current joined value. Pair with value for controlled usage"
            },
            {
              "name": "value",
              "type": "string",
              "default": "undefined",
              "required": false,
              "description": "Controlled value — pair with onChange. Omit to let the component manage its own state"
            },
            {
              "name": "defaultValue",
              "type": "string",
              "default": "",
              "required": false,
              "description": "Initial value for uncontrolled usage"
            },
            {
              "name": "autoClearOnFailure",
              "type": "boolean",
              "default": true,
              "description": "Clear the boxes and refocus the first one after the failure animation"
            },
            {
              "name": "disabled",
              "type": "boolean",
              "default": false,
              "required": false,
              "description": "Disable all boxes. Inputs are also locked automatically while validate is pending"
            },
            {
              "name": "autoFocus",
              "type": "boolean",
              "default": false,
              "required": false,
              "description": "Focus the first box on mount"
            },
            {
              "name": "label",
              "type": "string",
              "default": "One-time password",
              "required": false,
              "description": "Accessible name announced for the group of boxes"
            }
          ],
          "previewHint": "Type or paste the code 123456 to see the success wave — anything else triggers the red double-pulse and shake. Use the DialKit panel to reshape the ripple, swap the caret to block or underscore, and recolor it.",
          "features": [
            "Declarative validation: pass validate(code) returning boolean or Promise<boolean> — the component shows a pending ring while the promise is in flight, then runs the success or failure choreography and fires onSuccess / onFailure",
            "Paste and SMS autofill aware: pasting a full code fills every box from the first, and autocomplete=\"one-time-code\" lets mobile keyboards inject the code from a text message",
            "Configurable caret: bar uses the native caret with your color; block and underscore render a fake blinking caret that restarts solid on every keystroke, exactly like a native one",
            "Controlled or uncontrolled — pass value + onChange, or nothing at all and the component manages its own state",
            "Failure shake ships as plain CSS that the shadcn CLI merges into your globals.css on install, with shake distance exposed as a prop via a CSS custom property",
            "Accessible: role=group with per-box labels, aria-live announcements for checking / accepted / incorrect, and aria-invalid during failure",
            "Respects prefers-reduced-motion — ripples become instant color states, the shake and caret blink are disabled",
            "The ripple engine (RippleBorder) installs as a sibling file, ready to reuse for text inputs and search fields"
          ]
        }
      ]
    },
    {
      "slug": "button",
      "name": "Button",
      "category": "button",
      "shadcnBase": "button",
      "tags": [
        "button",
        "cta",
        "hover",
        "click",
        "sparkle",
        "particles",
        "confetti",
        "scramble",
        "offset-path",
        "whimsical"
      ],
      "description": "Animated buttons built on top of the shadcn/ui Button, each exploring a different animation pattern — a hover-triggered sparkle trail, a click-triggered confetti burst, and a hover-triggered scramble-text reveal.",
      "createdAt": "2026-07-17T00:00:00Z",
      "variations": [
        {
          "name": "whimsical",
          "displayName": "Whimsical",
          "componentName": "WhimsicalButton",
          "tier": "free",
          "description": "On hover or focus, five color-shifted sparkle particles launch along the button's border via CSS offset-path, each with its own speed, scale and landing spot.",
          "animationType": "keyframes",
          "dependencies": [
            "motion/react"
          ],
          "registryDependencies": [
            "craftui-base"
          ],
          "props": [
            {
              "name": "mainHue",
              "type": "number",
              "default": 260,
              "min": 0,
              "max": 360,
              "step": 1,
              "description": "Hue (0-360) driving --main-color and every particle's derived color"
            },
            {
              "name": "decoSize",
              "type": "number",
              "default": 7,
              "min": 3,
              "max": 16,
              "step": 1,
              "description": "Base width of each sparkle particle, in px"
            },
            {
              "name": "timing",
              "type": "number",
              "default": 1.5,
              "min": 0.4,
              "max": 3,
              "step": 0.1,
              "description": "Base travel duration in seconds — each particle multiplies this by its own speed variation"
            }
          ],
          "previewHint": "Hover (or tab to focus) the button to see the five sparkle particles launch along the border.",
          "features": [
            "Particles travel via CSS offset-path: border-box — they literally ride the button's own rounded outline, no hand-tuned path math",
            "Each particle is an independent config (color, scale, end-offset, end-translate, speed) driving a data array — no per-particle conditional logic",
            "Colors use CSS relative-color syntax (oklch(from var(--main-color) ...)) so a single mainHue prop repaints every particle consistently",
            "Opacity follows a life-cycle keyframe (fade in, hold, fade out) so particles never pop in or out abruptly",
            "Wraps shadcn/ui's Button (ghost variant) rather than replacing it — accepts every standard Button prop",
            "Skips all particle motion under prefers-reduced-motion; the border color change remains instant"
          ]
        },
        {
          "name": "confetti",
          "displayName": "Confetti Burst",
          "componentName": "ConfettiButton",
          "tier": "free",
          "description": "On click, a burst of color-varied confetti pieces launches from the button center — each with its own angle, distance and rotation — arcing up and falling back down under simulated gravity.",
          "animationType": "keyframes",
          "dependencies": [
            "motion/react"
          ],
          "registryDependencies": [
            "craftui-base"
          ],
          "props": [
            {
              "name": "particleCount",
              "type": "number",
              "default": 14,
              "min": 4,
              "max": 40,
              "step": 1,
              "description": "Number of confetti pieces launched per click"
            },
            {
              "name": "baseHue",
              "type": "number",
              "default": 200,
              "min": 0,
              "max": 360,
              "step": 1,
              "description": "Base hue (0-360) the confetti palette's hue offsets are applied to"
            },
            {
              "name": "spread",
              "type": "number",
              "default": 60,
              "min": 20,
              "max": 160,
              "step": 5,
              "description": "Max travel distance in px before gravity pulls pieces back down"
            },
            {
              "name": "duration",
              "type": "number",
              "default": 0.8,
              "min": 0.3,
              "max": 2,
              "step": 0.1,
              "description": "Total burst duration in seconds"
            }
          ],
          "previewHint": "Click the button to fire a confetti burst. Click repeatedly — each click stacks its own independent burst.",
          "features": [
            "Impulse-triggered on click, not continuous on hover — a one-shot celebratory animation rather than an ambient loop",
            "Each particle gets an independent random angle, distance, rotation, size and shape (rect or circle), driving a data array — no per-particle conditional logic",
            "y-axis uses a 3-keyframe rise-then-fall curve (easeOut up, easeIn down) to simulate gravity, while x and rotation animate in a single easeOut pass",
            "Colors cycle through a fixed set of hue offsets from a single baseHue prop, giving a multi-color mix instead of monochrome confetti",
            "Bursts are independent and stack — clicking again mid-animation launches a second burst without cancelling the first",
            "Wraps shadcn/ui's Button rather than replacing it — accepts every standard Button prop, including variant",
            "Skips the burst entirely under prefers-reduced-motion; the click still fires onClick"
          ]
        },
        {
          "name": "scramble",
          "displayName": "Scramble Text",
          "componentName": "ScrambleButton",
          "tier": "free",
          "description": "On hover or focus, the label's characters cycle rapidly through random glyphs before locking in to the real text one character at a time, left to right — a decode-style reveal built entirely from content substitution, no positional or color motion.",
          "animationType": "keyframes",
          "dependencies": [
            "motion/react"
          ],
          "registryDependencies": [
            "craftui-base"
          ],
          "props": [
            {
              "name": "label",
              "type": "string",
              "default": "Whimsy",
              "description": "The button's label — scrambled on hover, then revealed character-by-character"
            },
            {
              "name": "frameDuration",
              "type": "number",
              "default": 35,
              "min": 15,
              "max": 100,
              "step": 5,
              "description": "Milliseconds between scramble frames"
            },
            {
              "name": "staggerFrames",
              "type": "number",
              "default": 3,
              "min": 1,
              "max": 10,
              "step": 1,
              "description": "Frames each subsequent character waits before locking in — controls how pronounced the left-to-right reveal looks"
            }
          ],
          "previewHint": "Hover (or tab to focus) the button to watch the label scramble and resolve left to right.",
          "features": [
            "Purely typographic — no particles, movement or color change, just the label's characters substituting over time",
            "Each character locks in at a later frame than the one before it (index * staggerFrames), producing the left-to-right decode reveal from a single interval loop",
            "Spaces are preserved as-is and never scrambled, so multi-word labels stay legible mid-animation",
            "Re-hovering mid-scramble restarts cleanly — the previous interval is cleared before a new one starts",
            "Wraps shadcn/ui's Button rather than replacing it — accepts every standard Button prop, including variant",
            "Skips scrambling entirely under prefers-reduced-motion; the label is shown as-is"
          ]
        }
      ]
    },
    {
      "slug": "alert-dialog",
      "name": "Alert Dialog",
      "category": "overlay",
      "shadcnBase": "alert-dialog",
      "tags": [
        "alert-dialog",
        "dialog",
        "modal",
        "overlay",
        "confirmation",
        "base-ui",
        "animation",
        "clip-path",
        "spring",
        "blur"
      ],
      "description": "A confirmation dialog built on Base UI's accessible AlertDialog primitive. Every variation uses a different entrance technique — scale, blur focus-in, spring overshoot, iOS zoom, CRT unroll, a parting curtain, or an action-reactive exit that branches on Agree vs Disagree. All are theme-token driven so they flip cleanly between light and dark, and every surface is responsive with type that scales up on larger screens.",
      "createdAt": "2026-07-26T00:00:00Z",
      "variations": [
        {
          "name": "scale",
          "displayName": "Scale",
          "componentName": "ScaleAlertDialog",
          "tier": "free",
          "animationType": "ease",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 110,
              "min": 80,
              "max": 800,
              "step": 10,
              "description": "Enter/exit transition duration in milliseconds"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "Keep editing",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "Discard",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The popup scales up from 95% while fading in; adjust the duration with the DialKit panel.",
          "features": [
            "Enters by scaling the popup from 95% to 100% while cross-fading opacity, driven by the data-[starting-style] hook on an ease-out tween — compositor-only (transform + opacity)",
            "Breaks enter/exit symmetry: the exit drops the scale entirely and fades out on a fixed ~70ms opacity tween, decoupled from the enter with an important-flagged duration so tuning entrance speed never slows the dismiss",
            "Cross-fades the backdrop independently of the popup — a longer fade-in, a ~70ms fade-out — so the motion stays anchored on the card",
            "Renders fully from theme tokens (popover / border / primary / muted) and adapts responsively: full-width with column-stacked buttons on mobile, capped at max-w-md with a right-aligned row and a stepped-up title on ≥sm",
            "Inherits Base UI AlertDialog semantics — focus trap, focus restoration on close, role=\"alertdialog\", and aria-labelledby / aria-describedby wired from Title and Description; the trigger carries a focus-visible ring"
          ]
        },
        {
          "name": "blur",
          "displayName": "Blur",
          "componentName": "BlurAlertDialog",
          "tier": "free",
          "animationType": "ease",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 130,
              "min": 100,
              "max": 200,
              "step": 10,
              "description": "Blur focus-in duration in milliseconds"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "Stay",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "Sign out",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The card resolves out of a 16px blur while fading in — like a lens pulling focus.",
          "features": [
            "Resolves the popup out of a 16px blur while fading in — a state-driven transition on the filter and opacity properties (a filter/paint effect, not compositor-only), like a lens pulling focus; the exit reverses back into blur",
            "Layers a frosted-glass surface (translucent fill + backdrop-blur) tuned per theme through dark: overrides, with two ambient fuchsia / sky color blobs sitting behind the glass via a large blur radius for depth without touching content contrast",
            "Drives the blur-in duration from a duration prop; the exit mirrors the enter",
            "Aligns the primary action to the leading edge, sets body copy to relaxed leading, and gives the buttons a concentric rounded-xl against the rounded-3xl surface; padding and button stacking adapt on mobile",
            "Inherits Base UI AlertDialog semantics — focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description; the trigger shows a focus-visible ring"
          ]
        },
        {
          "name": "slam",
          "displayName": "Slam",
          "componentName": "SlamAlertDialog",
          "tier": "free",
          "animationType": "spring",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 320,
              "min": 200,
              "max": 900,
              "step": 10,
              "description": "Overshoot slam (enter) duration in milliseconds"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "Nope",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "Do it!",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The card drops in with a back-out spring, overshooting before it settles.",
          "features": [
            "Splits motion across two layers: the outer popup cross-fades opacity while an inner wrapper carries the transform via group-data-[starting-style] / [ending-style] hooks, keeping the animation off the centering translate",
            "Enters on a back-out (overshoot) cubic-bezier — the inner card scales up from 80% with a -40px translateY, overshooting before it settles; the enter duration is a duration prop",
            "Breaks enter/exit symmetry: the exit shrinks to 96% on a plain ease-out with no overshoot at a fixed ~160ms, so dismissal is quieter and faster than the slam-in",
            "Renders the neo-brutalist look with hard offset box-shadows and borders in var(--foreground) so the whole block inverts in dark mode; the yellow / lime accent buttons translate down and drop their shadow on the active press state",
            "Accessibility: honors prefers-reduced-motion (motion-reduce guards zero the transitions on backdrop, popup, and inner card so enter and exit are instant) and inherits Base UI's focus trap, focus restore, role=\"alertdialog\", and Title / Description-driven aria labelling"
          ]
        },
        {
          "name": "ios",
          "displayName": "iOS",
          "componentName": "IosAlertDialog",
          "tier": "free",
          "animationType": "spring",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 350,
              "min": 150,
              "max": 800,
              "step": 10,
              "description": "Spring settle (enter) duration in milliseconds"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "Don't Allow",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "Allow",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The card enters at 118% and springs down to rest, iOS system-alert style.",
          "features": [
            "Mirrors the iOS system alert: an inner card zooms from 118% down to rest on a back-out (overshoot) cubic-bezier while the outer popup cross-fades — motion split across two layers via group-data hooks, with the spring-settle enter duration driven by a duration prop",
            "Breaks enter/exit symmetry: the exit shrinks to 95% on a plain ease-out with no overshoot at ~200ms, faster than the ~350ms spring-in",
            "Renders a frosted popover surface (translucent fill + backdrop-blur) at a fixed 270px width with hairline dividers between stacked full-width buttons; the action reads in iOS blue with an active-press background tint",
            "Respects prefers-reduced-motion (motion-reduce guards make enter and exit instant) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description"
          ]
        },
        {
          "name": "terminal",
          "displayName": "Terminal",
          "componentName": "TerminalAlertDialog",
          "tier": "free",
          "animationType": "clip-path",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 350,
              "min": 150,
              "max": 900,
              "step": 10,
              "description": "Clip-path unroll (enter) duration in milliseconds"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Command shown after the prompt"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Warning body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "[esc] abort",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "[y] execute",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The popup unrolls vertically from a center line, CRT power-on style.",
          "features": [
            "Powers on like a CRT: clip-path unrolls the popup from inset(50% 0 50% 0) — a collapsed center line — out to inset(0) on an ease-out cubic-bezier, while opacity ramps in over roughly the first 40% of the clip so the reveal reads as the screen warming up (clip-path is a paint-level animation, not compositor-only)",
            "Breaks enter/exit symmetry: the exit re-rolls to the center line on a decoupled, faster ~220ms transition",
            "Keeps a self-contained green-on-black identity regardless of the site theme (only the trigger uses theme tokens); includes monospace type, faux traffic-light window dots, a $-prompt title with a blinking block cursor, and a [WARN]-tagged description",
            "Respects prefers-reduced-motion on two fronts — motion-reduce zeroes the unroll transition on popup and backdrop, and stops the infinite cursor pulse via animate-none — and inherits Base UI's focus trap, role=\"alertdialog\", and Title / Description aria labelling"
          ]
        },
        {
          "name": "curtain",
          "displayName": "Curtain",
          "componentName": "CurtainAlertDialog",
          "tier": "free",
          "animationType": "clip-path",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 400,
              "min": 200,
              "max": 1200,
              "step": 10,
              "description": "Clip-path curtain (enter) duration in milliseconds"
            },
            {
              "name": "eyebrow",
              "type": "string",
              "default": "Confirmation",
              "description": "Small uppercase label above the title"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "cancelLabel",
              "type": "string",
              "default": "Never mind",
              "description": "Dismiss button label"
            },
            {
              "name": "actionLabel",
              "type": "string",
              "default": "Confirm cancellation",
              "description": "Confirm button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when the action button is pressed"
            }
          ],
          "previewHint": "Click the trigger to open. The popup parts horizontally from its center line like a stage curtain.",
          "features": [
            "Parts like a stage curtain: clip-path opens the popup horizontally from inset(0 50% 0 50%) — a collapsed center line — out to inset(0) on an ease-out cubic-bezier; no opacity is animated, only the clip (a paint-level animation, not compositor-only)",
            "Breaks enter/exit symmetry: the curtain draws shut on a decoupled, faster transition than it opens",
            "Sets an editorial treatment — serif title and body, an uppercase letter-spaced eyebrow, and a short hairline rule — on full theme-token surfaces so the paper flips to ink in dark mode, with type stepping up from text-xl to text-2xl on ≥sm",
            "Respects prefers-reduced-motion (motion-reduce zeroes the clip transition on popup and backdrop) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and Title / Description-driven aria labelling"
          ]
        },
        {
          "name": "reactive",
          "displayName": "Reactive",
          "componentName": "ReactiveAlertDialog",
          "tier": "free",
          "animationType": "spring",
          "dependencies": [
            "@base-ui-components/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "duration",
              "type": "number",
              "default": 200,
              "min": 80,
              "max": 500,
              "step": 10,
              "description": "Enter / Agree-exit duration in milliseconds"
            },
            {
              "name": "disagreeDuration",
              "type": "number",
              "default": 150,
              "min": 100,
              "max": 200,
              "step": 5,
              "description": "Disagree tumble-away duration in milliseconds"
            },
            {
              "name": "disagreeAngle",
              "type": "number",
              "default": 3,
              "min": 0,
              "max": 25,
              "step": 1,
              "description": "Disagree rotation in degrees"
            },
            {
              "name": "disagreeShift",
              "type": "number",
              "default": 220,
              "min": 0,
              "max": 240,
              "step": 5,
              "description": "Disagree vertical drop in pixels"
            },
            {
              "name": "title",
              "type": "string",
              "description": "Dialog title"
            },
            {
              "name": "description",
              "type": "string",
              "description": "Supporting body copy"
            },
            {
              "name": "disagreeLabel",
              "type": "string",
              "default": "Disagree",
              "description": "Reject button label"
            },
            {
              "name": "agreeLabel",
              "type": "string",
              "default": "Agree",
              "description": "Accept button label"
            },
            {
              "name": "onAction",
              "type": "() => void",
              "description": "Fires when Agree is pressed"
            }
          ],
          "previewHint": "Click the trigger to open, then choose. Agree exits with a snappy scale-down; Disagree tips from its top-left corner and tumbles away.",
          "features": [
            "Branches the exit on the action: a controlled Base UI AlertDialog (open / onOpenChange) plus an exit-mode state set before close pick which closing animation runs, so the same dialog leaves two different ways",
            "Enters by scaling the inner card from 95% with an opacity fade on an ease-out curve; Agree mirrors that exactly — a snappy scale-down and fade — so accepting reverses the entrance",
            "Disagrees with a tumble: the card pivots from transform-origin top-left, rotating and dropping via translateY before fading, on an ease-in ('gravity') cubic-bezier that accelerates as it falls; the tumble's angle, drop distance, and duration are props",
            "Splits centering from animation across two layers (outer popup translates to center and fades, inner card carries the transform) so the exit rotate/translate never fights the centering translate; the exit mode resets to neutral on every open, keeping entrances and Escape / backdrop dismissals snappy",
            "Respects prefers-reduced-motion (motion-reduce guards make both exits instant) and inherits Base UI's focus trap, focus restoration, role=\"alertdialog\", and aria-labelledby / aria-describedby from Title and Description"
          ]
        }
      ]
    },
    {
      "slug": "accordion",
      "name": "Accordion",
      "category": "disclosure",
      "shadcnBase": "accordion",
      "tags": [
        "accordion",
        "disclosure",
        "animation",
        "ease",
        "spring",
        "drawer",
        "clip-path",
        "reveal",
        "faq"
      ],
      "description": "Animated accordion built on top of shadcn/ui. Replaces the CSS height transition with Framer Motion easing — fully accessible, keyboard navigable, drop-in ready.",
      "createdAt": "2026-06-29T00:00:00Z",
      "variations": [
        {
          "name": "clip",
          "displayName": "Clip",
          "componentName": "ClipAccordion",
          "tier": "free",
          "description": "Two-layer card reveal — the question clips away via clip-path in the chosen direction, exposing the answer beneath. Click the answer to return.",
          "animationType": "ease",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "direction",
              "type": "select",
              "options": [
                "horizontal",
                "vertical"
              ],
              "default": "horizontal",
              "description": "Direction the question clips away — horizontal (left) or vertical (up)"
            },
            {
              "name": "ease",
              "type": "ease",
              "default": [
                0.32,
                0.72,
                0,
                1
              ],
              "description": "Cubic-bezier easing curve for the clip animation"
            },
            {
              "name": "duration",
              "type": "number",
              "default": 0.45,
              "min": 0.1,
              "max": 1.5,
              "step": 0.01,
              "description": "Duration of the open animation in seconds (close runs at 60%)"
            }
          ],
          "previewHint": "Click a question to see it clip away and reveal the answer. Click the answer to return. Toggle between horizontal and vertical with the direction prop.",
          "features": [
            "Two-layer card: question sits as an absolute overlay on top of the answer — no height measurement needed, answer's rendered height sets the card size",
            "Exit is faster than enter — question clips away at 60% of the open duration so returning feels deliberate, not rushed",
            "Direction is a single prop: horizontal wipes right-to-left, vertical wipes bottom-to-top — same constants object, no conditional logic in the animation",
            "Pointer-events swap atomically: question layer is pointer-events:none when clipped, answer layer is pointer-events:none when covered",
            "Respects prefers-reduced-motion — all clip-path and translate animations disabled instantly",
            "Fully accessible: AccordionPrimitive.Trigger handles aria-expanded and keyboard navigation"
          ]
        },
        {
          "name": "drawer",
          "displayName": "Drawer",
          "componentName": "DrawerAccordion",
          "tier": "free",
          "description": "Each trigger lifts with a 3D spring when open, and the content slides up from behind it. Spring stiffness and damping are fully configurable via the DialKit panel.",
          "animationType": "spring",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "stiffness",
              "type": "number",
              "default": 220,
              "min": 40,
              "max": 600,
              "step": 10,
              "description": "Spring stiffness — higher values snap faster"
            },
            {
              "name": "damping",
              "type": "number",
              "default": 22,
              "min": 5,
              "max": 60,
              "step": 1,
              "description": "Spring damping — lower values add bounce, higher values settle immediately"
            }
          ],
          "previewHint": "To adjust the spring feel, use the DialKit panel to change stiffness (snappier vs. looser) and damping (bouncy vs. settled).",
          "features": [
            "Trigger lifts with a 3D perspective tilt when open — rotateX + y offset combine to create a physical card-lifting effect",
            "Content slides from y: -24 with the same spring as the trigger, so both feel coupled and intentional",
            "Exit is a fast ease-in snap — closing should feel decisive, not linger",
            "Chevron rotates 180° on open with a separate duration transition — icon state change is simpler than a spring",
            "whileHover lifts the card slightly even when closed — affordance that this is interactive",
            "Respects prefers-reduced-motion — all springs and transforms disabled instantly",
            "Fully accessible: keyboard navigation and ARIA attributes come from Radix unchanged"
          ]
        },
        {
          "name": "ease",
          "displayName": "Ease",
          "componentName": "EaseAccordion",
          "tier": "free",
          "description": "Content panel opens and closes with a cubic-bezier easing curve. Use the DialKit easing editor to draw any curve — ease-out, ease-in-out, or a custom bezier.",
          "animationType": "ease",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "ease",
              "type": "ease",
              "default": [
                0.215,
                0.61,
                0.355,
                1
              ],
              "description": "Cubic-bezier easing curve for the height animation"
            },
            {
              "name": "duration",
              "type": "number",
              "default": 0.26,
              "min": 0.1,
              "max": 1.5,
              "step": 0.01,
              "description": "Animation duration in seconds"
            }
          ],
          "previewHint": "To change the easing curve, open the DialKit easing editor and drag the handles to shape the cubic-bezier curve.",
          "features": [
            "Exit is faster than enter — closing should always feel decisive, users want content gone, not lingering",
            "Open plays as two beats: height snaps first, content fades in after a fixed gap — prevents text appearing while space is still forming",
            "Icon morphs with ease-in-out, content opens with ease-out — different curves for different roles: one is transforming in place, the other is entering the screen",
            "Opacity uses linear easing — at short durations easing curves on opacity are imperceptible, linear is honest",
            "Respects prefers-reduced-motion — all animations are disabled instantly when the user has reduced motion enabled in their OS",
            "Fully accessible: keyboard navigation and ARIA attributes come from Radix unchanged"
          ]
        }
      ]
    }
  ],
  "version": "1.0.0",
  "lastUpdated": "2026-07-27T14:58:26.479Z"
}