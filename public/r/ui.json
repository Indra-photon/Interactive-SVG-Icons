{
  "components": [
    {
      "slug": "otp-input",
      "name": "OTP Input",
      "category": "input",
      "tags": [
        "otp",
        "input",
        "verification",
        "2fa",
        "one-time-code",
        "ripple",
        "svg",
        "caret",
        "form"
      ],
      "description": "One-time password input with a living SVG ripple border. Every keystroke nudges the outline, correct codes send a green wave across the boxes, wrong codes bounce red and shake. Validation, carets and all motion parameters are prop-driven.",
      "createdAt": "2026-07-07T00:00:00Z",
      "variations": [
        {
          "name": "ripple",
          "displayName": "Ripple",
          "componentName": "RippleOtp",
          "tier": "free",
          "description": "Each box wears an SVG outline that behaves like a liquid surface — typing nudges it, moving to the next box hands the ripple off toward its right edge. Pass a validate function and the component choreographs the pending ring, the staggered success wave and the red shake by itself, then calls your onSuccess / onFailure.",
          "animationType": "ripple",
          "dependencies": [
            "motion/react"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "length",
              "type": "number",
              "default": 6,
              "min": 4,
              "max": 8,
              "step": 1,
              "description": "Number of character boxes"
            },
            {
              "name": "mode",
              "type": "select",
              "options": [
                "numeric",
                "alphanumeric"
              ],
              "default": "numeric",
              "description": "Which characters the boxes accept — alphanumeric uppercases as you type"
            },
            {
              "name": "masked",
              "type": "boolean",
              "default": false,
              "description": "Render entered characters as dots"
            },
            {
              "name": "rippleOnType",
              "type": "boolean",
              "default": false,
              "description": "Animate the border on every keystroke — off by default, so the ripple only plays for pending / success / failure"
            },
            {
              "name": "pendingRing",
              "type": "boolean",
              "default": false,
              "description": "Oscillating ring while an async validate is pending — off by default, pending shows as border color only"
            },
            {
              "name": "caretColor",
              "type": "string",
              "default": "#3B82F6",
              "description": "Caret color — drives the native bar caret and the fake block / underscore carets"
            },
            {
              "name": "caretShape",
              "type": "select",
              "options": [
                "bar",
                "block",
                "underscore"
              ],
              "default": "bar",
              "description": "Caret style — bar is the native caret; block and underscore are rendered carets with a native-feeling blink"
            },
            {
              "name": "staggerMs",
              "type": "number",
              "default": 70,
              "min": 30,
              "max": 200,
              "step": 5,
              "description": "Delay between each box's green pulse in the success wave, in ms"
            },
            {
              "name": "maxAmplitude",
              "type": "number",
              "default": 5,
              "min": 3,
              "max": 12,
              "step": 0.5,
              "description": "Ripple height ceiling in px"
            },
            {
              "name": "bumpWidth",
              "type": "number",
              "default": 0.1,
              "min": 0.05,
              "max": 0.25,
              "step": 0.01,
              "description": "How localized the ripple bulge is, as a fraction of the box perimeter"
            },
            {
              "name": "decay",
              "type": "number",
              "default": 0.06,
              "min": 0.02,
              "max": 0.2,
              "step": 0.005,
              "description": "How fast ripples settle — higher is snappier"
            },
            {
              "name": "radius",
              "type": "number",
              "default": 14,
              "min": 4,
              "max": 22,
              "step": 1,
              "description": "Corner radius in px, shared by the box and its ripple outline"
            },
            {
              "name": "strokeWidth",
              "type": "number",
              "default": 1.5,
              "min": 1,
              "max": 3,
              "step": 0.25,
              "description": "Ripple outline stroke width in px"
            },
            {
              "name": "shakeDistance",
              "type": "number",
              "default": 6,
              "min": 2,
              "max": 14,
              "step": 1,
              "description": "Failure shake travel in px"
            },
            {
              "name": "validate",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => boolean | Promise<boolean> — called when all boxes are filled. Return or resolve true for success, false for failure; the component shows the pending border color while a promise is in flight, then plays the result animation"
            },
            {
              "name": "onSuccess",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired after the green success wave finishes. Put your post-verification action here (navigate, submit, close modal)"
            },
            {
              "name": "onFailure",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired after the red pulse and shake finish. By default the boxes have already been cleared and refocused (see autoClearOnFailure)"
            },
            {
              "name": "onComplete",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(code: string) => void — fired once every time all boxes become filled, before validate runs"
            },
            {
              "name": "onChange",
              "type": "function",
              "default": "undefined",
              "required": false,
              "description": "(value: string) => void — fired on every edit with the current joined value. Pair with value for controlled usage"
            },
            {
              "name": "value",
              "type": "string",
              "default": "undefined",
              "required": false,
              "description": "Controlled value — pair with onChange. Omit to let the component manage its own state"
            },
            {
              "name": "defaultValue",
              "type": "string",
              "default": "",
              "required": false,
              "description": "Initial value for uncontrolled usage"
            },
            {
              "name": "autoClearOnFailure",
              "type": "boolean",
              "default": true,
              "description": "Clear the boxes and refocus the first one after the failure animation"
            },
            {
              "name": "disabled",
              "type": "boolean",
              "default": false,
              "required": false,
              "description": "Disable all boxes. Inputs are also locked automatically while validate is pending"
            },
            {
              "name": "autoFocus",
              "type": "boolean",
              "default": false,
              "required": false,
              "description": "Focus the first box on mount"
            },
            {
              "name": "label",
              "type": "string",
              "default": "One-time password",
              "required": false,
              "description": "Accessible name announced for the group of boxes"
            }
          ],
          "previewHint": "Type or paste the code 123456 to see the success wave — anything else triggers the red double-pulse and shake. Use the DialKit panel to reshape the ripple, swap the caret to block or underscore, and recolor it.",
          "features": [
            "Declarative validation: pass validate(code) returning boolean or Promise<boolean> — the component shows a pending ring while the promise is in flight, then runs the success or failure choreography and fires onSuccess / onFailure",
            "Paste and SMS autofill aware: pasting a full code fills every box from the first, and autocomplete=\"one-time-code\" lets mobile keyboards inject the code from a text message",
            "Configurable caret: bar uses the native caret with your color; block and underscore render a fake blinking caret that restarts solid on every keystroke, exactly like a native one",
            "Controlled or uncontrolled — pass value + onChange, or nothing at all and the component manages its own state",
            "Failure shake ships as plain CSS that the shadcn CLI merges into your globals.css on install, with shake distance exposed as a prop via a CSS custom property",
            "Accessible: role=group with per-box labels, aria-live announcements for checking / accepted / incorrect, and aria-invalid during failure",
            "Respects prefers-reduced-motion — ripples become instant color states, the shake and caret blink are disabled",
            "The ripple engine (RippleBorder) installs as a sibling file, ready to reuse for text inputs and search fields"
          ]
        }
      ]
    },
    {
      "slug": "accordion",
      "name": "Accordion",
      "category": "disclosure",
      "shadcnBase": "accordion",
      "tags": [
        "accordion",
        "disclosure",
        "animation",
        "ease",
        "spring",
        "drawer",
        "clip-path",
        "reveal",
        "faq"
      ],
      "description": "Animated accordion built on top of shadcn/ui. Replaces the CSS height transition with Framer Motion easing — fully accessible, keyboard navigable, drop-in ready.",
      "createdAt": "2026-06-29T00:00:00Z",
      "variations": [
        {
          "name": "clip",
          "displayName": "Clip",
          "componentName": "ClipAccordion",
          "tier": "free",
          "description": "Two-layer card reveal — the question clips away via clip-path in the chosen direction, exposing the answer beneath. Click the answer to return.",
          "animationType": "ease",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "direction",
              "type": "select",
              "options": [
                "horizontal",
                "vertical"
              ],
              "default": "horizontal",
              "description": "Direction the question clips away — horizontal (left) or vertical (up)"
            },
            {
              "name": "ease",
              "type": "ease",
              "default": [
                0.32,
                0.72,
                0,
                1
              ],
              "description": "Cubic-bezier easing curve for the clip animation"
            },
            {
              "name": "duration",
              "type": "number",
              "default": 0.45,
              "min": 0.1,
              "max": 1.5,
              "step": 0.01,
              "description": "Duration of the open animation in seconds (close runs at 60%)"
            }
          ],
          "previewHint": "Click a question to see it clip away and reveal the answer. Click the answer to return. Toggle between horizontal and vertical with the direction prop.",
          "features": [
            "Two-layer card: question sits as an absolute overlay on top of the answer — no height measurement needed, answer's rendered height sets the card size",
            "Exit is faster than enter — question clips away at 60% of the open duration so returning feels deliberate, not rushed",
            "Direction is a single prop: horizontal wipes right-to-left, vertical wipes bottom-to-top — same constants object, no conditional logic in the animation",
            "Pointer-events swap atomically: question layer is pointer-events:none when clipped, answer layer is pointer-events:none when covered",
            "Respects prefers-reduced-motion — all clip-path and translate animations disabled instantly",
            "Fully accessible: AccordionPrimitive.Trigger handles aria-expanded and keyboard navigation"
          ]
        },
        {
          "name": "drawer",
          "displayName": "Drawer",
          "componentName": "DrawerAccordion",
          "tier": "free",
          "description": "Each trigger lifts with a 3D spring when open, and the content slides up from behind it. Spring stiffness and damping are fully configurable via the DialKit panel.",
          "animationType": "spring",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "stiffness",
              "type": "number",
              "default": 220,
              "min": 40,
              "max": 600,
              "step": 10,
              "description": "Spring stiffness — higher values snap faster"
            },
            {
              "name": "damping",
              "type": "number",
              "default": 22,
              "min": 5,
              "max": 60,
              "step": 1,
              "description": "Spring damping — lower values add bounce, higher values settle immediately"
            }
          ],
          "previewHint": "To adjust the spring feel, use the DialKit panel to change stiffness (snappier vs. looser) and damping (bouncy vs. settled).",
          "features": [
            "Trigger lifts with a 3D perspective tilt when open — rotateX + y offset combine to create a physical card-lifting effect",
            "Content slides from y: -24 with the same spring as the trigger, so both feel coupled and intentional",
            "Exit is a fast ease-in snap — closing should feel decisive, not linger",
            "Chevron rotates 180° on open with a separate duration transition — icon state change is simpler than a spring",
            "whileHover lifts the card slightly even when closed — affordance that this is interactive",
            "Respects prefers-reduced-motion — all springs and transforms disabled instantly",
            "Fully accessible: keyboard navigation and ARIA attributes come from Radix unchanged"
          ]
        },
        {
          "name": "ease",
          "displayName": "Ease",
          "componentName": "EaseAccordion",
          "tier": "free",
          "description": "Content panel opens and closes with a cubic-bezier easing curve. Use the DialKit easing editor to draw any curve — ease-out, ease-in-out, or a custom bezier.",
          "animationType": "ease",
          "dependencies": [
            "motion/react",
            "radix-ui"
          ],
          "registryDependencies": [
            "accordion"
          ],
          "props": [
            {
              "name": "ease",
              "type": "ease",
              "default": [
                0.215,
                0.61,
                0.355,
                1
              ],
              "description": "Cubic-bezier easing curve for the height animation"
            },
            {
              "name": "duration",
              "type": "number",
              "default": 0.26,
              "min": 0.1,
              "max": 1.5,
              "step": 0.01,
              "description": "Animation duration in seconds"
            }
          ],
          "previewHint": "To change the easing curve, open the DialKit easing editor and drag the handles to shape the cubic-bezier curve.",
          "features": [
            "Exit is faster than enter — closing should always feel decisive, users want content gone, not lingering",
            "Open plays as two beats: height snaps first, content fades in after a fixed gap — prevents text appearing while space is still forming",
            "Icon morphs with ease-in-out, content opens with ease-out — different curves for different roles: one is transforming in place, the other is entering the screen",
            "Opacity uses linear easing — at short durations easing curves on opacity are imperceptible, linear is honest",
            "Respects prefers-reduced-motion — all animations are disabled instantly when the user has reduced motion enabled in their OS",
            "Fully accessible: keyboard navigation and ARIA attributes come from Radix unchanged"
          ]
        }
      ]
    }
  ],
  "version": "1.0.0",
  "lastUpdated": "2026-07-09T23:44:51.764Z"
}