{
  "sections": [
    {
      "slug": "team-cluster-01",
      "name": "Team Cluster 01",
      "category": "Know My Team",
      "tags": [
        "team",
        "section",
        "about",
        "people",
        "grid",
        "hover"
      ],
      "description": "A team section built around the avatar cluster — copy and a live detail panel on one side, a hatched grid of faces on the other. Hovering or selecting a face pops a role badge beside it and rewrites the panel; phones drop the cluster for a real list.",
      "createdAt": "2026-08-05T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Team Cluster 01",
          "tier": "free",
          "description": "A team section built around the avatar cluster. Perfet to use to showcase your team details in a website.  ",
          "animationType": "hover",
          "dependencies": [
            "motion",
            "@hugeicons/react",
            "@hugeicons/core-free-icons"
          ],
          "registryDependencies": [
            "https://www.craftui.space/r/avatar-default.json"
          ],
          "props": [
            {
              "name": "members",
              "type": "TeamMember[]",
              "required": false,
              "description": "The team. Extends the avatar block's AvatarMember with optional `bio` and `links`. Defaults to a six-person studio."
            },
            {
              "name": "members[].icon",
              "type": "HugeIcon",
              "required": false,
              "description": "HugeIcons icon shown at the leading edge of that person's row. Optional and unset by default for custom members — six copies of one generic user glyph is decoration, so a row with nothing specific to show renders without one."
            },
            {
              "name": "members[].bio",
              "type": "string",
              "required": false,
              "description": "One short phrase shown under the name in the role row, ~40 characters. It is clamped to a single line so the list stays about as tall as the square cluster beside it."
            },
            {
              "name": "members[].links",
              "type": "{ label: string; href: string }[]",
              "required": false,
              "description": "Links rendered under the bio for the active member."
            },
            {
              "name": "eyebrow",
              "type": "string",
              "required": false,
              "description": "Small label above the heading, uppercased and letterspaced in CSS — pass it in natural case. Defaults to \"Know my team\"."
            },
            {
              "name": "heading",
              "type": "ReactNode",
              "required": false,
              "description": "Section heading. Defaults to \"The people behind the work\"."
            },
            {
              "name": "body",
              "type": "ReactNode",
              "required": false,
              "description": "Paragraph under the heading."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the section element."
            }
          ],
          "componentName": "TeamCluster01",
          "previewHint": "Pick a row under the copy or a face on the right — either one drives the other",
          "features": [
            "Ships its own namespaced colour tokens, each defined as the theme token with a literal fallback, so the section inherits a host theme where one exists and stands alone where none does — and never overwrites the consumer's base tokens",
            "Colour comes from the theme's semantic tokens rather than hardcoded neutrals, so the section themes with the app it is installed into",
            "Each row leads with an icon chosen for that specific role — a compass, a paint board, a source-code mark — rather than one generic glyph repeated six times, which would be decoration; it also gives the row the affordance plain text never had",
            "Row icons are `currentColor` at 1.5px stroke, so they inherit the row's active colour with no second asset and carry the same optical weight as the 14px label beside them",
            "Row name and description sit on `foreground/70` and `muted-foreground` rather than neutral-400/600, which measured ~2.6:1 on white and ~2.3:1 on near-black — under WCAG AA for text that small",
            "The cluster stays a presentation block and reports its active member outward through `onActiveChange`, so the section owns layout and copy while the block keeps owning the grid — no fork, one implementation",
            "One cluster at every width — same 5×5, same cells, same arrangement, only the tile size changing — so the phone and the desktop show the same picture rather than two layouts that have to be learned separately",
            "The grid's gutter steps down on small screens, which is the one place a phone can win pixels per tile back without altering the layout",
            "Below 1280 the badge is the only detail surface, so `renderBadge` puts name and role in the pill; at 1280 and up the badge is hidden and the copy list takes over",
            "The role pills and the cluster are two views of one state, not two components kept in sync — the grid runs controlled, so picking a role and clicking a face are literally the same write",
            "Each row runs role left, name right, justified apart and divided by the same 45° hatch at the same 6px pitch as the placeholder shells — a heavier ink, because a 4px band needs it to read — which ties the two halves together without repeating the dashed outline",
            "Row descriptions are clamped to one line: at two lines every row grows ~14px, and six of those made the copy column visibly taller than the square cluster it sits beside",
            "Hover previews and click commits, so a touch user can select a member at all; the committed selection is what the panel falls back to when the pointer leaves",
            "The detail panel is a single DOM node moved between grid areas by `grid-template-areas`, not two copies shown and hidden — it is aria-live, and a duplicate would announce every change twice",
            "At ≥1280px the role badge moves off the photo and into the panel under the copy: the panel is already in the reading path there, so a badge over the tile would repeat it and cover the face it names. Below 1280 the layout stacks and the panel falls well under the cluster, so the on-tile badge earns its place again",
            "That breakpoint swap is one CSS rule against a `data-role-badge` hook on the block, not a resize listener — no breakpoint state to hydrate, no re-render on resize",
            "Section entrance borrows the cluster's own easing and ring stagger instead of introducing a second timing vocabulary, so heading, faces and panel read as one piece"
          ],
          "responsive": true
        }
      ]
    },
    {
      "slug": "feature-gallery-02",
      "name": "Feature Gallery 02",
      "category": "feature-section",
      "tags": [
        "feature",
        "section",
        "gallery",
        "scroll",
        "parallax",
        "image",
        "lenis",
        "travel",
        "showcase"
      ],
      "description": "A sticky scroll-driven gallery with a parallax image strip on the right and animated destination info on the left, connected by a flight-path arc divider.",
      "createdAt": "2026-07-02T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Feature Gallery 02",
          "tier": "free",
          "description": "Five travel destinations revealed through a sticky scroll. The right panel stacks images in a weighted parallax strip; the left panel slides in destination info driven by scroll position.",
          "animationType": "scroll-driven",
          "dependencies": [
            "motion",
            "lenis",
            "@hugeicons/react",
            "@hugeicons/core-free-icons",
            "next"
          ],
          "registryDependencies": [],
          "props": [],
          "componentName": "FeatureGallery02",
          "previewHint": "Scroll down to navigate through destinations",
          "features": [
            "Lenis smooth scroll drives the entire layout via a single useScroll hook",
            "Image strip uses scale + Y + rotate transforms so each image slots into a weighted parallax stack",
            "Arc SVG divider renders a dashed flight path; a plane icon follows via offset-path and flips direction based on scroll velocity",
            "Left panel text slots use AnimatePresence mode='wait' spring transitions — no layout shift",
            "Stat values animate with useSpring so numbers count up smoothly on each destination change"
          ],
          "responsive": true
        }
      ],
      "published": false
    },
    {
      "slug": "feature-gallery-01",
      "name": "Feature Gallery 01",
      "category": "feature-section",
      "tags": [
        "feature",
        "section",
        "gallery",
        "expand",
        "click",
        "interior"
      ],
      "description": "A centered feature section — eyebrow, heading, subheading and a two-button CTA row — above a strip of image cards that expand on click. Written for a studio or portfolio site where the work needs to carry the section.",
      "createdAt": "2026-08-02T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Feature Gallery 01",
          "tier": "free",
          "description": "Cards rise into place on scroll, then the clicked card widens around a stationary image while the rest narrow. Heights never change; the label and scrim fade in last.",
          "animationType": "click",
          "dependencies": [
            "motion"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "images",
              "type": "GalleryImage[]",
              "required": false,
              "description": "Cards in the strip. Each is { label, description?, src, tint } — description is an optional line rendered under the label on the expanded card, and tint is the solid colour shown behind the photo while it loads and in the collapsed sliver."
            },
            {
              "name": "eyebrow",
              "type": "string",
              "required": false,
              "description": "Small label above the heading, uppercased and letterspaced in CSS — pass it in natural case. Defaults to \"What we do\"."
            },
            {
              "name": "heading",
              "type": "React.ReactNode",
              "required": false,
              "description": "Section heading, rendered as an h2. Accepts JSX, so individual words can be wrapped in a span to colour or style them — e.g. heading={<>Spaces designed <span className=\"text-primary\">around how you live</span></>}."
            },
            {
              "name": "subheading",
              "type": "React.ReactNode",
              "required": false,
              "description": "Supporting paragraph below the heading. Accepts JSX, so parts of the sentence can be emphasised with a span or a link."
            },
            {
              "name": "ctaLabel",
              "type": "string",
              "required": false,
              "description": "Primary button label. Defaults to \"Book a consultation\"."
            },
            {
              "name": "ctaHref",
              "type": "string",
              "required": false,
              "description": "Primary button destination."
            },
            {
              "name": "secondaryLabel",
              "type": "string",
              "required": false,
              "description": "Secondary outline button label. Defaults to \"View all projects\"."
            },
            {
              "name": "secondaryHref",
              "type": "string",
              "required": false,
              "description": "Secondary button destination."
            },
            {
              "name": "defaultOpenIndex",
              "type": "number | null",
              "required": false,
              "description": "Zero-based index of the card expanded when the section mounts. Defaults to 4 (the fifth card). Pass null for none; an index past the end of `images` is treated as null."
            },
            {
              "name": "countNoun",
              "type": "string",
              "required": false,
              "description": "Noun used in the \"Showing 5 of 10 projects\" line, which appears only if a breakpoint renders fewer cards than were passed. Defaults to \"projects\"."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the section element."
            }
          ],
          "componentName": "FeatureGallery01",
          "previewHint": "Click any card to expand it; click it again to collapse. Arrow keys move across the strip",
          "features": [
            "Each card carries an inset 1px hairline — pure black at 10% on light, pure white at 10% on dark — drawn as a top-most overlay rather than an inset box-shadow (which paints below the image) or an outline (which the card already spends on focus-visible), so a pale photo never bleeds into the page background",
            "Entrance is staged, not one fade: the strip fades in at 120ms and cards rise from 10px, staggered 30ms apart",
            "Opening a card animates width only — the photo is fixed at the expanded size and centred, so the card widens around a stationary image instead of rescaling it",
            "Card heights are constant, so clicking never disturbs the strip's baseline or the surrounding layout",
            "Collapsing runs in reverse order: scrim and caption leave together in 140ms on an ease-in, then geometry waits 150ms, so text is fully gone before a moving card edge can clip it",
            "Opening stages four beats rather than one fade — scrim at 165ms, label at 225ms, description at 275ms, then a pair of corner crop marks at 310ms once the card has settled. Geometry, fades and exits each use their own easing curve",
            "Corner brackets frame the open card, arced to the same radius as the card and shipped as the --gallery-frame token. They sit on the diagonal from 768px up, move to both top corners below that where the description wraps into the bottom-right, and mirror in RTL so they always stay clear of the caption",
            "Card geometry switches across four breakpoints (34/40/48/54px collapsed). Below 768px the strip scrolls horizontally instead of dropping cards, so every image stays reachable on a phone",
            "Arrow keys, Home and End move across the strip and open each card as they go; the opened card scrolls itself into view once it has finished widening. Each card reports aria-expanded rather than posing as a tab",
            "Respects prefers-reduced-motion — the entrance and expand sequences resolve instantly to their end state",
            "Card focus is a paired inset ring — a white band with a dark band outside it — drawn inside the card so it never overlaps a neighbour. The pairing is deliberate: a single white ring disappears on pale photography, which a gallery cannot predict",
            "Scrim and label ship as --gallery-scrim and --gallery-label tokens. They stay white-on-dark in both themes by design: the label sits on a photograph, not on the page surface, so following the page theme would make it vanish in light mode."
          ],
          "responsive": true
        }
      ]
    },
    {
      "slug": "feature-ai-01",
      "name": "Feature AI 01",
      "category": "feature-section",
      "tags": [
        "feature",
        "section",
        "ai",
        "infrastructure",
        "canvas",
        "generative",
        "isometric",
        "autoplay",
        "hover"
      ],
      "description": "A split feature section for AI, GPU and developer-infrastructure marketing pages: an isometric compute-stack diagram anchors the header, a list of capability words sits on the left, and an olive card on the right answers each one with body copy and a generative canvas loop. The card walks itself through all six capabilities on autoplay so a scroller sees the whole story, then hands control over permanently the moment someone hovers, focuses or taps a row. Useful anywhere a platform has more claims than a hero can hold and each claim needs its own sentence without stacking six cards down the page.",
      "createdAt": "2026-08-12T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Feature AI 01",
          "tier": "free",
          "description": "A split feature section for AI, GPU and developer-infrastructure marketing pages: an isometric compute-stack diagram anchors the header, a list of capability words sits on the left, and an olive card on the right answers each one with body copy and a generative canvas loop. The card walks itself through all six capabilities on autoplay so a scroller sees the whole story, then hands control over permanently the moment someone hovers, focuses or taps a row. Useful anywhere a platform has more claims than a hero can hold and each claim needs its own sentence without stacking six cards down the page.",
          "animationType": "hover",
          "dependencies": [
            "motion"
          ],
          "registryDependencies": [
            "https://www.craftui.space/r/badge-default.json",
            "https://www.craftui.space/r/button-default.json",
            "https://www.craftui.space/r/card-default.json",
            "https://www.craftui.space/r/separator-default.json"
          ],
          "props": [
            {
              "name": "capabilities",
              "type": "Capability[]",
              "required": false,
              "description": "The rows in the list and the content of the card. Each is { word, art, copy } — word is the list label, copy is the card paragraph, and art is a loop from the exported FEATURE_AI_ART bundle (flow, pulse, columns, lorenzOrbit, dipole, lorenzSlice) or any custom { id, name, period, fps?, draw } of your own. Defaults to the exported DEFAULT_CAPABILITIES; an empty array falls back to it."
            },
            {
              "name": "autoplayMs",
              "type": "number",
              "required": false,
              "description": "Milliseconds each capability holds while the section is autoplaying. Defaults to 4000 — long enough to read the copy, short enough that a skimmer sees two or three before scrolling past."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the section element."
            }
          ],
          "componentName": "FeatureAi01",
          "previewHint": "Wait and the card cycles on its own; hover or tap any capability to take over",
          "features": [
            "Autoplays only while at least 35% of the section is in view and only until the first interaction, so the five capabilities nobody would have hovered still get read — and touch users, who have no hover at all, see the whole set",
            "Hover previews the row while the pointer rests on it and a click commits it; the preview waits 100ms so sweeping the cursor down six rows fires one crossfade instead of six that cannot finish",
            "Every card visual is a canvas loop drawn from a closed-form period — flow fields, gaussian pulse fronts, dipole fields and a numerically-found Lorenz periodic orbit whose residual drift is spread across the period so the seam is invisible",
            "The canvas is inverted and composited with mix-blend-screen under a radial mask, so the olive gradient tints the marks and the art has no edge where the patch ends",
            "Swaps are staggered art → label → copy, and exits run their own faster ease-in curve so a leaving element is gone before the next one arrives",
            "The active-row marker is a shared layoutId dot, so it travels between rows instead of disappearing and reappearing",
            "Card grain is a generated feTurbulence tile rather than a downloaded texture — no image request, and fractalNoise keeps it even instead of clumping",
            "Inactive row ink is #6f6f6b rather than a pale grey, clearing the 3:1 large-text contrast floor on all six interactive rows",
            "MotionConfig reducedMotion='user' collapses every spring and crossfade in the section, and the canvas loops freeze on their opening frame under prefers-reduced-motion",
            "The isometric header diagram is computed entirely in world space through one projection function — no coordinate is placed by eye — and every solid is painted back to front so the stack occludes itself without z-index",
            "shadcn Button, Card, Badge and Separator are restyled entirely at the call site through cn(), so the section drops into a project without touching the primitives"
          ],
          "responsive": true
        }
      ]
    }
  ],
  "version": "1.0.0",
  "lastUpdated": "2026-08-13T08:44:49.984Z"
}