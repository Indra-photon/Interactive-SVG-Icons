{
  "name": "circle-spinner-wipe-default",
  "type": "registry:ui",
  "tier": "free",
  "dependencies": [
    "framer-motion"
  ],
  "files": [
    {
      "path": "components/loaders/circle-spinner-wipe/default.tsx",
      "content": "'use client';\n\nimport { motion } from 'framer-motion';\n\ninterface CircleSpinnerWipeProps {\n  width?: number;\n  height?: number;\n  color?: string;\n  backgroundColor?: string;\n  thickness?: number;\n  isAnimating?: boolean;\n}\n\nexport function CircleSpinnerWipe({\n  width = 60,\n  height = 60,\n  color = \"currentColor\",\n  backgroundColor = \"#dddddd\",\n  thickness = 10,\n  isAnimating = true,\n}: CircleSpinnerWipeProps) {\n  const centerX = width / 2;\n  const centerY = height / 2;\n  const radius = Math.min(width, height) / 2 - thickness / 2;\n  const circumference = 2 * Math.PI * radius;\n\n  return (\n    <svg\n      width={width}\n      height={height}\n      viewBox={`0 0 ${width} ${height}`}\n      fill=\"none\"\n      xmlns=\"http://www.w3.org/2000/svg\"\n      aria-label=\"Loading\"\n      role=\"img\"\n    >\n      {/* Background track circle */}\n      <circle\n        cx={centerX}\n        cy={centerY}\n        r={radius}\n        stroke={backgroundColor}\n        strokeWidth={thickness}\n        fill=\"none\"\n      />\n      {/* Animated wipe circle - starts from top (rotated -90deg) */}\n      <motion.circle\n        cx={centerX}\n        cy={centerY}\n        r={radius}\n        stroke={color}\n        strokeWidth={thickness}\n        fill=\"none\"\n        strokeLinecap=\"butt\"\n        strokeDasharray={circumference}\n        animate={{\n          strokeDashoffset: [circumference, 0],\n          rotate: [-90, -90],\n        }}\n        transition={{\n          strokeDashoffset: {\n            duration: 2,\n            repeat: isAnimating ? Infinity : 0,\n            ease: 'linear',\n            times: [0, 1],\n          },\n        }}\n        style={{ originX: `${centerX}px`, originY: `${centerY}px`, rotate: -90 }}\n      />\n    </svg>\n  );\n}\n",
      "type": "registry:ui",
      "target": ""
    }
  ],
  "meta": {
    "displayName": "Circle Spinner Wipe - Circle Spinner Wipe",
    "description": "Two concentric rings where the foreground ring wipes clockwise from 0% to 100% over the background track",
    "category": "loaders",
    "tags": [
      "spinner",
      "circle",
      "wipe",
      "rotate",
      "simple",
      "smooth",
      "progress",
      "ring"
    ],
    "animationType": "loop"
  }
}