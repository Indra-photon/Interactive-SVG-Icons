{
  "name": "grid-clock-default",
  "type": "registry:ui",
  "tier": "free",
  "dependencies": [
    "framer-motion"
  ],
  "registryDependencies": [
    "http://localhost:3000/r/craftui-base.json"
  ],
  "files": [
    {
      "path": "components/craftui/loaders/grid-clock/default.tsx",
      "content": "'use client';\n\nimport { motion } from 'motion/react';\n\ninterface GridClockProps {\n  width?: number;\n  height?: number;\n  color?: string;\n  dotSize?: number;\n  isAnimating?: boolean;\n  duration?: number;\n  ease?: any;\n  gridSize?: number;\n}\n\nexport function GridClock({\n  width = 100,\n  height = 100,\n  color = 'currentColor',\n  dotSize = 16,\n  isAnimating = true,\n  duration = 1.4,\n  ease = 'easeInOut',\n  gridSize = 3,\n}: GridClockProps) {\n  const n = Math.max(2, Math.min(6, gridSize));\n  const stride = 100 / (n + 1);\n  const dotRadius = Math.min(dotSize, stride * 0.85) / 2;\n\n  // Clockwise sweep from 12 o'clock; center dot(s) fire last\n  const mid = (n - 1) / 2;\n  const maxDelay = duration * 0.69;\n  const delays = Array.from({ length: n * n }, (_, i) => {\n    const row = Math.floor(i / n), col = i % n;\n    const dr = -(row - mid), dc = col - mid;\n    if (dr === 0 && dc === 0) return maxDelay;\n    const angle = (Math.atan2(dc, dr) + 2 * Math.PI) % (2 * Math.PI);\n    return (angle / (2 * Math.PI)) * maxDelay;\n  });\n\n  return (\n    <svg\n      width={width}\n      height={height}\n      viewBox=\"0 0 100 100\"\n      fill=\"none\"\n      xmlns=\"http://www.w3.org/2000/svg\"\n      aria-label=\"Loading\"\n      role=\"img\"\n    >\n      {Array.from({ length: n }).map((_, row) =>\n        Array.from({ length: n }).map((_, col) => (\n          <motion.circle\n            key={`${row}-${col}`}\n            cx={stride + col * stride}\n            cy={stride + row * stride}\n            fill={color}\n            animate={{\n              r: [dotRadius * 0.4, dotRadius, dotRadius * 0.4],\n              opacity: [0.15, 1, 0.15],\n            }}\n            transition={{\n              duration: duration,\n              repeat: isAnimating ? Infinity : 0,\n              delay: delays[row * n + col],\n              ease: ease,\n              times: [0, 0.35, 1],\n            }}\n          />\n        ))\n      )}\n    </svg>\n  );\n}\n",
      "type": "registry:ui",
      "target": "~/components/craftui/loaders/grid-clock/default.tsx"
    }
  ],
  "meta": {
    "displayName": "Grid Clock - Grid Clock",
    "description": "Dots sweep clockwise from top-center (12 o'clock) through all 8 outer positions, then the center fires last",
    "category": "loaders",
    "tags": [
      "grid",
      "clock",
      "dots",
      "matrix",
      "clockwise",
      "radial"
    ],
    "animationType": "loop"
  }
}