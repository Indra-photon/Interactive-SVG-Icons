{
  "name": "ios-spinner-trail-default",
  "type": "registry:ui",
  "tier": "free",
  "dependencies": [
    "framer-motion"
  ],
  "files": [
    {
      "path": "components/loaders/ios-spinner-trail/default.tsx",
      "content": "\"use client\";\n\nimport { motion } from \"framer-motion\";\n\ninterface IosSpinnerTrailProps {\n  width?: number;\n  height?: number;\n  color?: string;\n  isAnimating?: boolean;\n}\n\nexport function IosSpinnerTrail({\n  width = 100,\n  height = 100,\n  color = \"currentColor\",\n  isAnimating = true,\n}: IosSpinnerTrailProps) {\n  const count = 12;\n  const opacities = [\n    1, 0.82, 0.64, 0.48, 0.34, 0.22, 0.13, 0.07, 0.03, 0.01, 0.005, 0.001,\n  ];\n\n  return (\n    <svg\n      width={width}\n      height={height}\n      viewBox=\"0 0 100 100\"\n      fill=\"none\"\n      xmlns=\"http://www.w3.org/2000/svg\"\n      aria-label=\"Loading\"\n      role=\"img\"\n    >\n      <motion.g\n        style={{ transformOrigin: \"50px 50px\" }}\n        animate={{ rotate: isAnimating ? 360 : 0 }}\n        transition={{\n          duration: 0.7,\n          repeat: isAnimating ? Infinity : 0,\n          ease: \"linear\",\n        }}\n      >\n        {Array.from({ length: count }).map((_, i) => (\n          <rect\n            key={i}\n            x={48.5}\n            y={26}\n            width={3}\n            height={10}\n            rx={1.5}\n            fill={color}\n            opacity={opacities[i]}\n            transform={`rotate(${(360 / count) * i}, 50, 50)`}\n          />\n        ))}\n      </motion.g>\n    </svg>\n  );\n}\n",
      "type": "registry:ui",
      "target": ""
    }
  ],
  "meta": {
    "displayName": "iOS Spinner Trail - Sharp Trail",
    "description": "Exponential opacity decay — only 4-5 spokes are visibly bright, the rest vanish sharply, creating an intense laser comet",
    "category": "loaders",
    "tags": [
      "apple",
      "ios",
      "macos",
      "spinner",
      "trail",
      "comet",
      "sharp",
      "exponential"
    ],
    "animationType": "loop"
  }
}