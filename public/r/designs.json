{
  "designs": [
    {
      "slug": "workflow-pipeline",
      "name": "Workflow Pipeline",
      "category": "diagram",
      "tags": [
        "design",
        "static",
        "diagram",
        "pipeline",
        "workflow",
        "automation",
        "connector",
        "settings",
        "toggle"
      ],
      "description": "A three-lane automation diagram: web, maps and AI sources on the left, a settings card in the middle, and the agents and booked confirmation they produce on the right. The elbow connectors are generated from the same coordinate table the nodes are positioned from, so every line terminates exactly on the edge it points at rather than near it. One design pixel is a fraction of the panel's own width — type, icons and toggles included — so the composition holds from a narrow masonry column to a full-width hero with no breakpoints. The toggles are drawn in a fixed state rather than wired to a handler: a still picture of a configured workflow, not a working control panel. Renders entirely on the server.",
      "createdAt": "2026-08-19T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Workflow Pipeline",
          "tier": "free",
          "description": "A three-lane automation diagram: web, maps and AI sources on the left, a settings card in the middle, and the agents and booked confirmation they produce on the right. Elbow connectors are generated from the same coordinate table the nodes are placed from, and every measurement is a fraction of the panel's own width, so type, icons and toggles keep their proportions at any size. Static — no hooks, no motion, zero JavaScript.",
          "animationType": "none",
          "dependencies": [
            "@hugeicons/react",
            "@hugeicons/core-free-icons"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "agents",
              "type": "[string, string]",
              "required": false,
              "description": "The two avatar photographs in the output column, top and bottom. Each carries a sound-wave badge marking it as an agent on a call. Defaults to two Unsplash portraits served through their CDN transform at 120px."
            },
            {
              "name": "status",
              "type": "string",
              "required": false,
              "description": "Label on the middle output pill — the one result the pipeline is drawn to produce. Defaults to \"Booked\"; the green tick badge is part of the mark, not conditional on the text."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the panel root. The panel is aspect-ratio driven at 624/252 and fills its container's width — it is also the container-query root, so overriding its width rescales everything inside it."
            }
          ],
          "componentName": "WorkflowPipeline",
          "width": 624,
          "height": 252
        }
      ]
    },
    {
      "slug": "upgrade-modal",
      "name": "Upgrade Modal",
      "category": "card",
      "tags": [
        "design",
        "static",
        "modal",
        "dialog",
        "upsell",
        "pricing",
        "gradient",
        "shadcn",
        "nova",
        "tokens"
      ],
      "description": "An upsell dialog drawn in its open state, built on the shadcn \"Nova\" preset — Stone neutrals, Blue primary, radius 0 — stated once as a token object so every colour, gap, size and type value on the card reads from the same source; the Paper and Figma originals bind the identical variables. A 200px hero carries the one non-token surface: the mesh shader from the design flattened to a linear ramp that runs indigo from the bottom-left to violet at the top-right, with a square 28px close mark pinned in its corner. Below it the header is a single row — title and a 20px primary pill held apart by space-between — with the description under both, then three feature rows whose 24px check tiles sit in a fixed lane so the copy column aligns, and a full-width primary action with a ghost dismiss centred beneath. Card edge is a 1px ring at foreground/10 rather than a border. Renders entirely on the server.",
      "createdAt": "2026-09-15T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Upgrade Modal",
          "tier": "free",
          "description": "An upsell dialog in its open state on the shadcn Nova preset (Stone, Blue, radius 0). Gradient hero with a square close mark, a title-and-badge row held apart by space-between, three check-tiled feature rows in a fixed lane, and a full-width primary action with a ghost dismiss. Every value on the card reads from one token object. Static — no hooks, no motion, zero JavaScript.",
          "animationType": "none",
          "dependencies": [],
          "registryDependencies": [],
          "props": [
            {
              "name": "title",
              "type": "string",
              "required": false,
              "description": "Dialog heading. Defaults to \"Upgrade to Paper Pro\"."
            },
            {
              "name": "description",
              "type": "ReactNode",
              "required": false,
              "description": "Line under the title, set in muted-foreground. Wraps across the full card width."
            },
            {
              "name": "badge",
              "type": "string",
              "required": false,
              "description": "Primary pill on the right of the title row. Defaults to \"2 months free\"; pass \"\" to remove it and the title keeps the full row."
            },
            {
              "name": "features",
              "type": "{ title: string; description: string }[]",
              "required": false,
              "description": "Feature rows. Each renders a 24px check tile in a fixed lane beside a medium title and a muted description; the list grows the card."
            },
            {
              "name": "ctaLabel",
              "type": "string",
              "required": false,
              "description": "Label on the full-width primary action. Defaults to \"Upgrade now · $12/mo\". Drawn, not wired — the piece is a still."
            },
            {
              "name": "dismissLabel",
              "type": "string",
              "required": false,
              "description": "Ghost action under the primary one. Defaults to \"Maybe later\"."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the dialog root. The card is capped at 496px wide and lays out in fixed pixels, which is the width the hero, the title/badge row and the feature lane were composed against."
            }
          ],
          "componentName": "UpgradeModal",
          "width": 592,
          "height": 718
        }
      ]
    },
    {
      "slug": "stacked-tower",
      "name": "Stacked Tower",
      "category": "illustration",
      "tags": [
        "design",
        "static",
        "illustration",
        "svg",
        "3d",
        "cylinder",
        "oklch",
        "annotation",
        "marketing"
      ],
      "description": "Four cylindrical units stacked into a deliberately unsteady tower, drawn entirely from ellipses that all obey one ratio — ry = ryRatio × rx — so height is only ever vertical offset between two identical shapes, with no perspective transform, rotation or skew anywhere in the file. Each unit derives every surface it has from a single OKLCH base walked down a ramp that adds chroma and rotates hue as it darkens, which is what keeps the shadows reading as coloured light rather than as grey. Ribs are spaced evenly in angle and then projected, so they bunch toward the silhouette and start on the curve of the top ellipse the way real corrugation does. Curved leader lines are derived from the shapes themselves rather than authored beside them, so they track the geometry when it is retuned. The whole drawing is one SVG with no hooks and no runtime — it renders on the server and ships no JavaScript.",
      "createdAt": "2026-09-02T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Stacked Tower",
          "tier": "free",
          "description": "Four cylindrical units stacked into a deliberately unsteady tower. Every ellipse obeys one camera ratio, so height reads as vertical offset rather than as tilt; every surface on a unit is derived from one OKLCH base walked down a ramp that gains chroma as it darkens; ribs are spaced in angle and projected, so they bunch toward the silhouette. The base drum runs off the bottom of the card, which is what makes the tower feel taller than its frame. Static — no hooks, no motion, zero JavaScript.",
          "animationType": "none",
          "dependencies": [],
          "registryDependencies": [],
          "props": [
            {
              "name": "geometry",
              "type": "Geometry",
              "required": false,
              "description": "Every measurement, colour and label in the drawing: the camera (ryRatio and stroke width), the shading palette (ramp, edge and contact opacities, rib step and blend), the annotation type scale and curve, the lid overhang, and the ordered list of units. Defaults to DEFAULT_GEOMETRY, which the file also exports. The one rule with no tolerance is camera.ryRatio — it applies to every ellipse at once, so changing it re-poses the whole tower, while overriding a single shape's ry would read as a tilted unit rather than a higher one."
            },
            {
              "name": "brand",
              "type": "string",
              "required": false,
              "description": "Wordmark beside the bird glyph in the masthead. Defaults to \"Cardinal\"."
            },
            {
              "name": "headline",
              "type": "string",
              "required": false,
              "description": "Centred line under the masthead, which is also the SVG's accessible title. Defaults to \"Your sales motion, built by you\"."
            },
            {
              "name": "id",
              "type": "string",
              "required": false,
              "description": "Prefix for every generated SVG id — the card clip, each cylinder's rib clip and edge gradient, and the title and description. SVG ids are document-global, so two instances on one page need two prefixes or the second silently steals the first's clip paths. Defaults to \"stacked-tower\"."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Classes on the root <svg>. The drawing is viewBox-driven at 1230×738 with preserveAspectRatio, so it fills whatever width it is given and keeps its proportions — there are no pixel dimensions on the element."
            }
          ],
          "componentName": "StackedTower",
          "width": 1230,
          "height": 738
        }
      ]
    },
    {
      "slug": "rewards-card",
      "name": "Rewards Card",
      "category": "card",
      "tags": [
        "design",
        "static",
        "card",
        "onboarding",
        "illustration",
        "hand-drawn",
        "blur",
        "shadcn",
        "nova",
        "tokens"
      ],
      "description": "An onboarding card on the shadcn \"Nova\" preset — Stone neutrals, Blue primary, radius 0 — sharing its token object with the Upgrade Modal so the two read as one system. The blue haze across the top is the layer-blur trick rather than a gradient: one ellipse in chart-1 pushed 230px above the card and blurred by 90px, clipped by the card edge, so it fades into the white with nothing to tune. Three 96px tiles sit in a row with the middle one lifted 8px, each with a 4px inset white border and carrying a hand-drawn line illustration composed on a 96 grid in the reference style — single-weight ink, one off-white paper shape behind it, exactly one accent per tile. They read as a story: a watering can raising three coin-headed sprouts (grow your earnings), a wrapped gift box with a primary ribbon (the reward), and a cursor tapping a checklist with the first box ticked (the easy action). Centred copy beneath, subtext locked to 250px so it breaks on two lines, and a soft primary action — primary-foreground fill, primary text — spanning the card. Renders entirely on the server.",
      "createdAt": "2026-09-15T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Rewards Card",
          "tier": "free",
          "description": "An onboarding card on the shadcn Nova preset with a layer-blurred blue haze across the top, three inset-bordered tiles carrying hand-drawn ink illustrations — watering can and coin sprouts, gift box, tapped checklist — centred copy and a soft primary action. Every value reads from one token object shared with the Upgrade Modal. Static — no hooks, no motion, zero JavaScript.",
          "animationType": "none",
          "dependencies": [],
          "registryDependencies": [],
          "props": [
            {
              "name": "title",
              "type": "string",
              "required": false,
              "description": "Centred heading under the tiles. Defaults to \"Get rewarded fast\"."
            },
            {
              "name": "subtext",
              "type": "string",
              "required": false,
              "description": "Muted line under the title, capped at 250px so it breaks on two lines. Defaults to \"Choose and earn fast with these simple offers.\""
            },
            {
              "name": "ctaLabel",
              "type": "string",
              "required": false,
              "description": "Label on the soft primary action. Defaults to \"Show 10 easy offers\". Drawn, not wired."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the card root. Capped at 496px wide; the haze, the tile row and the subtext measure were composed against that width."
            }
          ],
          "componentName": "RewardsCard",
          "width": 592,
          "height": 500
        }
      ]
    },
    {
      "slug": "notes-card",
      "name": "Notes Card",
      "category": "card",
      "tags": [
        "design",
        "static",
        "card",
        "tasks",
        "todo",
        "list",
        "mobile",
        "oklch",
        "actions"
      ],
      "description": "A task list app screen, drawn inside a phone: a card that fills the display and carries its own tab bar, so the navigation belongs to the thing it navigates rather than floating beside it. Built on a design system stated once: two type pairs — 16px with medium, 14px with normal — where selection is signalled by surface and colour rather than by a third size, and letter-spacing rides on the type token so the same size is never set two ways in two components. Radii are concentric and the arithmetic is checkable: 16 on the card, 8 one padding step in, 4 two steps in. Rows are wells recessed with a 1px inset ring rather than a border, and each row ends in a rail of three action buttons whose boxes and gaps divide that rail exactly. The leading mark is a ring filled as a wedge in proportion to subtasks done, drawn on the icon set's own 24 grid at the shared stroke weight. Status pills use `text-box: trim-both cap alphabetic` to centre on their cap height rather than their line box, every control carries a 40px hit area as a pseudo-element that never overlaps its neighbour's, and transitions name the exact properties that change. Colour is OKLCH throughout at hue 264, with the ink value spent in exactly two places — the primary action and the selected tab. Renders entirely on the server.",
      "createdAt": "2026-09-03T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Notes Card",
          "tier": "free",
          "description": "A task list screen inside a phone shell — status bar, home indicator, and a card that fills the display between them. Header with a primary action, a segmented filter, and date-grouped rows carrying completion, status, counts and a rail of edit/complete/delete actions. Two type pairs, concentric 16/8/4 radii, 40px hit areas on every control, press feedback at 0.96, and transitions that name only the properties that move. The progress mark is a wedge drawn on the icon set's own grid, and the last row sits mid-swipe with its delete action revealed. Static — no hooks, no motion library, zero JavaScript.",
          "animationType": "none",
          "dependencies": [
            "@hugeicons/react",
            "@hugeicons/core-free-icons"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "time",
              "type": "string",
              "required": false,
              "description": "Clock in the status bar. Defaults to \"9:41\". Set in tabular figures so a different time cannot shift the radios beside it."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the scaling root. The device is fixed at a 390 × 844 screen inside a 12px bezel and is scaled to the caller's width by a viewBox, so it shrinks to fit a narrow column and stops at its natural 414px — set a `max-w-*` here to cap it smaller."
            }
          ],
          "componentName": "NotesCard",
          "width": 414,
          "height": 868
        }
      ]
    },
    {
      "slug": "insight-card",
      "name": "Insight Card",
      "category": "card",
      "tags": [
        "design",
        "static",
        "card",
        "marketing",
        "halftone",
        "dots",
        "generative",
        "oklch",
        "sphere"
      ],
      "description": "A marketing card whose lower half is a halftone field with a comet swept through it — the cone is a void carved out of the dots rather than a shape drawn over them, so the sphere at its head reads as having cleared its own path. One signed-distance function decides both which dots get removed and which get thinned at the edge, so the silhouette can never drift from the wake. Dots are bucketed into three OKLCH shades by their distance from that wake, which is what gives the flat grid depth. The whole field is generated at module scope from a deterministic value hash rather than Math.random, so the server and the client draw the identical card and hydration stays quiet. The sphere is a radial gradient with its focal point pushed toward a named light azimuth, and the CTA gradient is the dot ramp dropped 0.14 in lightness so its label clears AA at both ends. Renders entirely on the server.",
      "createdAt": "2026-08-19T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Insight Card",
          "tier": "free",
          "description": "A marketing card whose lower half is a halftone field with a comet swept through it — the cone is a void carved out of the dots, not a shape drawn over them. One signed-distance function decides both which dots are removed and which are feathered at the edge, and three OKLCH shades bucketed by distance from the wake give the flat grid depth. Generated deterministically at module scope, so server and client draw the identical card.",
          "animationType": "none",
          "dependencies": [
            "@hugeicons/react",
            "@hugeicons/core-free-icons"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "brand",
              "type": "string",
              "required": false,
              "description": "Wordmark beside the flower glyph, set in wide tracking. Defaults to \"NORTHBEAM\"."
            },
            {
              "name": "heading",
              "type": "ReactNode",
              "required": false,
              "description": "The headline. A node rather than a string on purpose — the default wraps two words in font-semibold against the medium weight around them, which is the emphasis the card is composed for."
            },
            {
              "name": "ctaLabel",
              "type": "string",
              "required": false,
              "description": "Label on the gradient pill. Defaults to \"Learn More\"; the arrow badge is part of the mark, not conditional on the text."
            },
            {
              "name": "href",
              "type": "string",
              "required": false,
              "description": "Destination for the CTA anchor. Defaults to \"#\"."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the card root. The card is capped at 362px wide and lays out in fixed pixels, which is the width the halftone field, the type scale and the two-line headline break were composed against."
            }
          ],
          "componentName": "InsightCard",
          "width": 362,
          "height": 628
        }
      ]
    },
    {
      "slug": "flight-card",
      "name": "Flight Card",
      "category": "card",
      "tags": [
        "design",
        "static",
        "card",
        "travel",
        "flight",
        "poster",
        "photo",
        "glass",
        "backdrop-blur"
      ],
      "description": "A full-bleed travel poster with the flight held behind a progressive blur that fades away at the card's midline — airport codes, cities and times either side of an airplane on a dashed route track, with the duration beneath it. The painted sky under the photograph doubles as a grade over it, so a photograph the component never chose still belongs to the card. Colour is OKLCH throughout, every fade is an eased gradient rather than a linear one, and the frame and its controls carry separate concentric radius systems. Every measurement is a fraction of the card's own width — blur radius, dash rhythm and icon included — so it holds its composition from a narrow column to a full-width hero with no breakpoints. Renders entirely on the server.",
      "createdAt": "2026-08-17T00:00:00Z",
      "variations": [
        {
          "name": "default",
          "displayName": "Flight Card",
          "tier": "free",
          "description": "A full-bleed travel poster with the flight held behind a progressive blur that fades away at the card's midline — airport codes, cities and times either side of an airplane on a dashed route track, with the duration beneath it. The painted sky under the photograph doubles as a grade over it, so a photograph the component never chose still belongs to the card. Colour is OKLCH throughout, every fade is an eased gradient rather than a linear one, and the frame and its controls carry separate concentric radius systems. Every measurement is a fraction of the card's own width — blur radius, dash rhythm and icon included — so it holds its composition from a narrow column to a full-width hero with no breakpoints. Renders entirely on the server.",
          "animationType": "none",
          "dependencies": [
            "@hugeicons/react",
            "@hugeicons/core-free-icons"
          ],
          "registryDependencies": [],
          "props": [
            {
              "name": "image",
              "type": "string",
              "required": false,
              "description": "Background photograph. Defaults to a San Francisco bridge from Pexels, served through their CDN transform (287KB rather than the 4.0MB original). Pass your own URL to own the asset, or \"\" to fall through to the painted sky alone — every text pair on the card is measured to pass APCA Lc 60 against either."
            },
            {
              "name": "from",
              "type": "FlightEndpoint",
              "required": false,
              "description": "Origin as { code, time, city }. Defaults to LHR / 10:30 AM / London."
            },
            {
              "name": "to",
              "type": "FlightEndpoint",
              "required": false,
              "description": "Arrival as { code, time, city }. Defaults to SFO / 1:20 PM / San Francisco."
            },
            {
              "name": "duration",
              "type": "string",
              "required": false,
              "description": "Time in the air, shown under the route mark — the one number that makes two clock times mean something. Written, not computed: the endpoint times are display strings in unknown timezones. Defaults to \"10h 50m\"."
            },
            {
              "name": "grade",
              "type": "\"none\" | \"tone\" | \"duotone\"",
              "required": false,
              "description": "How hard the card's palette is pushed onto the photograph, using the same sky gradient the card falls back to. \"tone\" (default) is a soft-light pass that keeps the photo's own hues — chroma survives at 98% — and unifies its tone; \"duotone\" is a colour pass that imposes the palette completely at no contrast cost, but flattens vivid colour to about 55% chroma; \"none\" leaves the photo alone. Ignored when there is no image."
            },
            {
              "name": "options",
              "type": "[string, string]",
              "required": false,
              "description": "The two labels in the segment control. Drawn, not wired — this is a still card, so the control shows a choice rather than offering one."
            },
            {
              "name": "activeOption",
              "type": "0 | 1",
              "required": false,
              "description": "Which of the two reads as chosen. Defaults to 0, so the chosen segment agrees with the artwork and the route."
            },
            {
              "name": "className",
              "type": "string",
              "required": false,
              "description": "Additional classes on the card root. The card is aspect-ratio driven at 3:4 and fills its container's width."
            }
          ],
          "componentName": "FlightCard",
          "width": 560,
          "height": 747
        }
      ]
    }
  ],
  "version": "1.0.0",
  "lastUpdated": "2026-09-15T11:59:12.467Z"
}